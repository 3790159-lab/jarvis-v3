# DEV-31: gitignored-состав, который обязан приехать в новый worktree.
#
# Зачем список, а не «скопировать всё лишнее»: worktree создаётся, чтобы
# снимать в нём baseline. Baseline, снятый ДРУГИМ набором файлов, — не
# baseline, а другое измерение (13.08: 22 ложных падения на отсутствующем
# `requisites.yaml`, и они читались как регресс кода).
#
# И зачем не «скопировать весь gitignore»: `.secrets/` — это Telethon-сессии,
# то есть полный доступ к аккаунту. Плодить их копии по каталогам ради зелёного
# pytest нельзя; тесты, которым они нужны, делают свои временные.
#
# Правило: сюда попадает только то, ЧЬЁ ОТСУТСТВИЕ КРАСНИТ ТЕСТ, и это
# ЗАМЕРЕНО, а не предположено. Каждая строка — glob относительно корня
# репозитория. `#` — комментарий.
#
# Секретные файлы кладутся ЖЁСТКОЙ ССЫЛКОЙ, а не копией (см. new_worktree.py):
# данные остаются в одном месте на диске, ротация ключа доезжает во все
# worktree сама, а протухшая копия ключа не притворяется живой.

# Платёжные реквизиты клиентов: их читают сторожа платежей, эскалации и
# панелей. Файл gitignored намеренно (чужие банковские данные), шаблон
# requisites.example.yaml трекается и подменой не годится — сторожа сверяют
# именно живой состав.
chatter/clients/*/requisites.yaml

# `app/env_bootstrap` читает корневой env-файл дерева ДО любых настроек. Без
# него `TELEGRAM_ALLOWED_CHAT_ID` пуст, обработчики callback молча отбивают
# нажатие, и 27 wiring-тестов краснеют, не сказав про окружение ни слова
# (замерено 15.08 полной суитой: 65 падений в worktree против 40 в живом
# дереве). Ещё два теста расходятся В ДРУГУЮ сторону: `test_face_swap`
# проверяет «без ключа обязано падать» и в дереве без ключа проходит. Неполный
# состав врёт в обе стороны, а не только «недостаёт».
#
# Способ — `link`: жёсткая ссылка, данные остаются одной записью на диске.
link .env
link .env.runpod
