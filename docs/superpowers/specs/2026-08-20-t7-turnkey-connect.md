# T7+: подключение клиента под ключ — ✅ ОК ВЛАДЕЛЬЦА 20.08

Статус: **спека утверждена 20.08 вечером, кода нет.** Продолжение
`chatter.onboard` (T1–T6, смержено 17.08, транк `d6389101`). T1–T6 собирают
КОНФИГ; здесь — всё, что между готовым конфигом и живым ботом, который
отвечает лиду.

## Решения владельца 20.08 (не переобсуждаются)

| # | решение | обоснование владельца |
|---|---|---|
| q1 | `active.yaml` не трогаем, держим сторожом Д13 | постановка была неверной, поправка §0-бис принята |
| q2 | **ЧЕТЫРЕ кода выхода** `0/1/2/3` | «жду тебя» и «сломано» требуют разной реакции; слипшись — приучают не смотреть на красное. Конвенция `onboard` подождёт |
| q3 | бандл: **вариант (б)** — маркер из строки «сессии в бандле», которую печатает сам экспорт | (а) напоминание без проверки через месяц становится фоном; (в) блокировка на том, чего проверить нельзя — уже видели, чем кончается |
| q4 | перед дрилом **останавливаться ВСЕГДА** | «$0.2–0.3 не деньги, но позавчера баланс кончился посреди прогона, и я узнал об этом от бота» — человек обязан видеть, что сейчас потратится |
| q5 | **отдельный `chatter.connect`** | `onboard` собирает файлы, `connect` поднимает живого клиента: разные права и разный радиус ошибки. Слитый модуль однажды подключит что-нибудь при сборке |
| q6 | согласие клиента — **отдельным шагом с фактом на диске** | бот читает ВСЮ личку аккаунта; это фиксируется, а не подразумевается. Формат: строка с датой и от кого получено |

Граница взята из §5.5 спеки T1–T6 дословно: «Живой дрил и деплой. `active.yaml`,
логин сессии, `/funnel_gate on`, рестарт раннера. Мерж ≠ деплой». Это ровно то,
что здесь и закрывается — кроме `active.yaml`, см. §0-бис.

---

## 0. Что чиним

Сегодня подключение живёт в `docs/chatter/ONBOARDING_MANUAL.md` — 9 разделов,
~15 команд, три из которых нельзя менять местами. Ярину подключали по нему
16–17.08: логин → токен → перешифровка → реестр → подъём → дрилы. Прошло
хорошо, и это ничего не доказывает: чеклист исполнял человек, который его же
и писал, в один заход, не отвлекаясь.

Три способа, которыми этот путь ломается, уже наблюдались:

1. **Порядок.** Сегодня, 20.08, трижды подряд: рестарт не в том месте убил бы
   здоровую панель, а `enabled: true` до валидного конфига дал бы гардиану
   бесконечный цикл подъёма. Порядок держится памятью исполнителя.
2. **Обрыв.** Сессии рвутся по несколько раз в день. Половина подключения —
   это состояние, которого нет ни в одном файле: «токен уже записал?
   перешифровал? бандл снимал?». Возобновление сегодня = пересказ по памяти.
3. **Двенадцать команд подряд.** Каждая печатает своё, и ни одна не знает, что
   было до неё. Пропущенный шаг обнаруживается на следующем — или у клиента.

**Цель:** ОДНА команда, которая доводит подключение до конца, останавливается
на первом несоответствии, называет действие и продолжает с того же места после
любого обрыва.

**Не цель:** заменить человека там, где решает вкус, доступ или закон — §5.

---

## 0-бис. 🔴 Поправка к постановке: `active.yaml` — ЛОЖНЫЙ КАНАЛ

В задаче названо «`active.yaml` + рестарт + приёмка». Автоматизировать
`active.yaml` **нельзя**, и не потому, что трудно, а потому, что подключения он
не касается. Факты, снятые сегодня:

* шапка самого файла: «⚠️ ГАРДИАНОМ ЭТОТ ФАЙЛ БОЛЬШЕ НЕ ИСПОЛЬЗУЕТСЯ»;
* `scripts/chatter_guardian_detached.ps1:338` — гардиан зовёт раннер как
  `chatter.telethon_run --personas <slug> --session … --db … --client <slug>`,
  то есть состав приходит **планом из реестра**, а `active.yaml` не читается
  вовсе;
* в том же скрипте (стр. 145–155) есть комментарий, объясняющий, почему из
  стартовой строки гардиана убрали упоминание состава: строка «печатала бы
  неизменное `active.yaml` / по первому слагу, то есть врала бы ровно там, где
  должна помогать».

`active.yaml` остаётся живым ровно для одного случая: ручной запуск
`python -m chatter.telethon_run` без аргументов, для разовой отладки. Подключение
клиента этим путём не идёт и идти не должно — ручной процесс умирает вместе с
сессией (урок 22.07, записан в §4 мануала).

**Значит:** источник истины подключения — `chatter/clients/registry.yaml`.
Автоматизируем запись туда. `active.yaml` не трогаем **ни на байт**, и это
записано отдельным сторожом (Д13): правка `active.yaml` во время подключения —
дефект, а не недоделка.

Класс уже знаком: [[jarvis-panel-db-source-is-the-live-runner]] — спека
чинила канал, которого живой процесс не читает.

---

## 1. Модель состояния: шаг = ПРОБА ФАКТА НА ДИСКЕ

Главное требование заказчика: подключение может прерваться посередине, и
состояние обязано восстанавливаться из фактов, а не из памяти процесса.

Поэтому у команды **нет своего файла состояния как источника истины**. Каждый
шаг — чистая функция:

```
probe(ctx) -> StepResult          # точная сигнатура — §12.2, здесь только модель
Verdict ∈ { ЗАКРЫТ, НЕ ЗАКРЫТ, ПРОТИВОРЕЧИЕ }
```

* **ЗАКРЫТ** — факт есть и согласован с соседями;
* **НЕ ЗАКРЫТ** — факта нет; шаг предстоит (кому — §3);
* **ПРОТИВОРЕЧИЕ** — факты спорят друг с другом (реестр включён, а конфига
  нет; в реестре одна сессия, на диске другая). Это дефект, не ожидание.

Журнал (`state/connect/<slug>.md`, дописывается после каждого шага) заводится
для ЧЕЛОВЕКА и **не читается пробами никогда**. Если журнал удалить, ни один
вердикт не изменится; если журнал подделать — тоже. Это прямое следствие
[[jarvis-two-numbers-for-one-thing]]: второе представление состояния молча
погасило бы первое. Сторож Д7 держит оба направления.

Отсюда же **идемпотентность**: повторный запуск команды не повторяет ни одного
действия, чей факт уже на диске. Особенно платного — §2.3.

---

## 2. Ворота: три вида, и у каждого названа цена

Ворота — не «проверка на всякий случай». Каждое стоит между действием и
конкретным ущербом, который уже наблюдался.

### 2.1. Предусловие (что должно быть ДО шага)

| ворота | что предотвращает |
|---|---|
| конфиг грузится НАСТОЯЩИМ `load_config` | `enabled: true` при битом конфиге = гардиан поднимает и роняет раннер по кругу |
| сессия открывается (`session_available`, правило `.enc` из `secret_loader`) | подъём в вечный рестарт: сессии нет, процесс умирает на старте |
| `.env.enc` в состоянии `in_sync` | `bootstrap_env` при наличии `.enc` читает ТОЛЬКО его: токен, записанный в plaintext `.env`, до процесса не доедет — ловушка Ярины 16.08 |
| имя env-переменной токена **пер-клиентское** и уникальное среди включённых | общий токен = два раннера в `getUpdates`, Telegram отдаёт long-poll одному, второй ловит 409, команды пульта ходят через раз **без ошибок в логе** |
| `session`/`db` не совпадают с другим ВКЛЮЧЁННЫМ клиентом | гонка за запись `.session` вплоть до разлогина аккаунта (валидация реестра это уже умеет — зовём её, а не повторяем) |
| `funnel_gate: false` на всём протяжении | подключение не имеет права открыть трафик; открывает владелец отдельной командой |

### 2.2. Доказательство (что должно быть ПОСЛЕ шага)

Шаг закрывается фактом, а не отсутствием ошибки:

* реестр — не «файл записан», а `registry_cli` вернул `runnable=true, error=null`;
* подъём — не «скрипт отработал», а `state/chatter_clients.json` даёт `alive`
  И heartbeat свежий. **`starting` за `alive` не считается** (Д10);
* токен доехал — строка `control-bot poller starting (isolated token)` в
  `logs/chatter_<slug>.log`. Именно она доказала перешифровку у Ярины;
* дрил — файл `state/drills/<ts>.md` и код возврата харнесса, а не «прогнали».

### 2.3. Деньги и радиус

* **Дрил платный** (~$0.20–0.30 за прогон, замер 17.08: $0.2287 при смете
  $0.2951). Смета печатается ДО старта. Второй прогон при существующем
  результате — только явным флагом. Повтор «на всякий случай» — это деньги
  клиента и посторонний трафик в его БД.
* **Подъём меряет catch-up радиус** — но НЕ своей копией: зовём
  `scripts/chatter_client.ps1 -Slug <slug> -Action start`, в который замер уже
  вшит (17.08, требование владельца). Отказ замера = остановка. `-Force` команда
  подключения **не подставляет никогда**: у нового клиента радиус нулевой по
  построению, а если он вдруг не нулевой — это и есть то, что надо прочитать
  глазами.

---

## 3. Карта шагов

Колонка «кто» — **А** автомат, **Ч** человек по конструкции (§5).
Колонка «факт» — то, что читает проба. Порядок менять нельзя; каждая строка
опирается на факт предыдущей.

| # | шаг | кто | факт на диске |
|---|---|---|---|
| S0 | среда: `TELEGRAM_API_ID/HASH`, `ANTHROPIC_API_KEY`, реестр парсится | **Ч** | значения в env-материале, `registry_cli` без `fatal` |
| S1 | конфиг собран T1–T6 | **Ч** | `build/onboard/<slug>/` + `report.json`, красных проверок нет |
| S2 | вычитка тона и правдоподобия | **Ч** | метка `REVIEWED` в каталоге сборки, после неё `--check` даёт rc 0 |
| S3 | решения по флагам и конфликтам брифа | **Ч** | каждый флаг, поднятый в `report.json` (подмножество `C12`/`C13`/`C16`), **назван** в тексте метки |
| S4 | конфиг перенесён в `chatter/clients/<slug>/` | А | каталог существует и грузится `load_config` |
| S5 | **согласие клиента на доступ к аккаунту** | **Ч** | `chatter/clients/<slug>/CONSENT.md`: дата + от кого получено |
| S6 | логин Telegram (код из SMS, 2FA) | **Ч** | `.secrets/<slug>.session(.enc)`, `session_available` = true |
| S7 | токен у BotFather → `.env` → перешифровка | **Ч** | пер-клиентская env-переменная непуста, `-Check` = `in_sync` |
| S8 | пересъём бандла секретов `.jrvbak` | **Ч** | маркер `state/connect/<slug>.bundle.txt` со строкой «сессии в бандле», в ней есть слаг |
| S9 | allowlist: владелец + дрил-контакт | А | `telegram.allowlist` в `settings.yaml` живого конфига |
| S10 | запись в `registry.yaml`, `enabled: false` | А | запись есть, `runnable` посчитан, `error=null` |
| S11 | подъём: `chatter_client.ps1 -Action start` | А | `enabled: true` в реестре + замер радиуса прошёл |
| S12 | приёмка живости | А | `alive` + свежий heartbeat + строка `isolated token` |
| S13 | первый дрил-прогон | А + **ворота денег** | `state/drills/<ts>.md` + код возврата |
| S14 | вычитка текста дрила глазами | **Ч** | харнесс не судит правдоподобие: `EXPECT_KEYS` закрыт |
| S15 | открытие трафика `/funnel_gate on confirm` | **Ч** | **вне команды навсегда** — §5.5 |

**Почему S0 и S1 помечены «человек», хотя проверяются автоматом.** Колонка
«кто» отвечает на вопрос «кто ЗАКРЫВАЕТ шаг», а не «кто проверяет». Секрет в
`.env` кладёт владелец; конфиг собирает отдельная команда `chatter.onboard`.
Автоматического действия у этих шагов нет, и выдумывать его нельзя: `connect`,
умеющий сам пересобрать конфиг, однажды пересоберёт вычитанный.

Отсюда инвариант, который держит код честным: **AUTO ⇔ у шага есть `act`**.
Шаг без действия — человеческий по определению, и его остановка обязана
называть точную команду (§12.4). Автоматических шагов ровно шесть: S4, S9,
S10, S11, S12, S13.

**S1 и S2 разведены не случайно.** `--check` даёт rc 0 только ВМЕСТЕ с меткой
(решение владельца q3 от 17.08), поэтому «rc 0» не может быть признаком S1 —
он уже содержит в себе S2. Проба S1 читает `report.json`: красных нет, флаги
могут быть. Иначе шаг «конфиг собран» был бы недостижим до вычитки и печатал бы
человеку «сделай то, что ты и так собирался», вместо «читай отчёт».

**S3 стоит объяснить отдельно.** «Человек принял решения по флагам» — суждение,
и проверить его нельзя. Но можно проверить МЕХАНИЧЕСКОЕ следствие: что в тексте
метки названы все флаги, которые поднял отчёт. Это не оценка решения, это
доказательство, что решение вообще принималось про каждый флаг, а не про те,
что попались на глаза. Метка уже сегодня несёт свободный текст («подпись под
выжимкой от 17.08, полный REPORT.md не читался») — форма не меняется, добавляется
только требование назвать id.

**S5 (согласие) стоит ПЕРЕД логином, и это не формальность.** Бот читает ВСЮ
личку аккаунта — мы просим доступ к личной переписке человека. Факт: одна
строка с датой и от кого получено. Проба проверяет ровно проверяемое: файл
есть, непуст, дата разбирается и не в будущем.

Сверку «согласие датировано РАНЬШЕ логина» я рассматривал и **отвергаю**:
единственная опора для даты логина — mtime session-файла, а он двигается при
каждой записи Telethon. Сторож на нём был бы то зелёным, то красным без связи
с предметом. Порядок держится порядком шагов, а не сверкой времён.

**S8 (маркер бандла), вариант (б) владельца.** Команда экспорта печатает
строку `сессии в бандле: ...` — её человек кладёт в маркер. Проба требует
ровно двух вещей: маркер существует и непуст, и в строке **назван слаг этого
клиента**. Это и есть тот факт, которого не давал вариант (а).

Чего проба НЕ делает: не сравнивает время маркера с временем session-файла.
Соблазн («бандл снят ДО логина и не несёт новой сессии») понятен, но опора
негодная — та же, что отвергнута в S5: mtime сессии двигается при каждой
записи Telethon, и после подъёма раннера (S11) такой сторож краснел бы на
здоровом подключении. Сторож, который меняет вердикт сам по себе, — это фон,
а не проверка.

Остаточный риск назван вслух: маркер доказывает, что бандл снимали и что в
нём была эта сессия, но не доказывает ни свежести бандла, ни того, что
носитель уехал с машины. Это потолок проверяемого; round-trip из §9 мануала
остаётся человеческим.

**S4: расхождение с `build/` ПОСЛЕ переноса — норма, а не ворота.** Человек
правит тон в живом конфиге, и это ровно то, ради чего §5.1 существует. Проба
S4 спрашивает «каталог есть и грузится», а разницу с `build/` печатает
СТРОКОЙ ОТЧЁТА. Ворота здесь превратили бы вычитку в дефект.

---

## 4. Один вход

```
python -m chatter.connect <slug>              # довести подключение до ближайших ворот
python -m chatter.connect <slug> --plan       # НИЧЕГО не делать: карта шагов и вердикты
python -m chatter.connect <slug> --drill-yes  # разрешить ОДИН платный дрил-прогон (S13)
python -m chatter.connect <slug> --drill-again  # повторный дрил при уже имеющемся результате
```

Флагов для «сделай только шаг N» нет намеренно: это и есть возврат к двенадцати
командам. Порядок выбирает код, человек выбирает — продолжать или нет.

`--drill-yes` — решение владельца q4: **перед дрилом останавливаемся всегда**,
даже на $0.2–0.3. Повод конкретный: позавчера баланс кончился посреди прогона,
и владелец узнал об этом от бота. Поэтому остановка печатает смету ДО списания,
а не после. Флаг разрешает ровно один прогон и не запоминается: следующий
запуск снова остановится.

Каждая остановка печатает ТРИ строки и ничего сверх:

```
ОСТАНОВ на S7: токен контрол-бота.
ПОЧЕМУ: CHATTER_CONTROL_BOT_TOKEN_ACME пуст. Общий токен с другим клиентом
        даёт 409 в getUpdates, и пульт отвечает через раз без ошибок в логе.
ЧТО СДЕЛАТЬ: возьми токен у @BotFather, добавь через .\scripts\add_secret.ps1,
        затем .\scripts\reencrypt_env.ps1; после этого повтори ту же команду.
```

Строка «повтори ту же команду» обязательна везде: вход один, и человек не
должен помнить, чем продолжать.

### Коды выхода

| код | значение |
|---|---|
| 0 | ГОТОВО: все автоматические шаги закрыты, осталась только команда владельца об открытии трафика |
| 3 | ЖДЁТ ЧЕЛОВЕКА: штатная остановка на шаге из §5 |
| 1 | ПРОТИВОРЕЧИЕ: факты спорят между собой — разбираться |
| 2 | НЕ СОСТОЯЛСЯ: не прочитали вход/факты, прогон ничего не доказал |

Отделять 3 от 1 нужно ради автоматики и ради человека: «жду тебя» и «сломано»
требуют разной реакции, а слипшись они приучают не смотреть на красное.
**Решение владельца q2: четыре кода, конвенция `onboard` подождёт.**

---

## 5. ЧЕГО НЕ АВТОМАТИЗИРУЕМ — ПО КОНСТРУКЦИИ

Список закрытый. Каждый пункт — не «руки не дошли», а «автоматизация здесь
означает подделку».

1. **Тон и правдоподобие** (S2, S14). Звучит ли это как живой администратор;
   не обещает ли лишнего; веришь ли реплике как клиент. Все проверяемые
   следствия уже вынуты в C1–C14 автоприёмки; остаток — суждение. Автомат здесь
   = LLM-судья над выходом LLM, и он согласится с собой. Попыток больше не
   делаем (решение записано в §5.1 спеки T1–T6).
2. **Решения по флагам и конфликтам брифа** (S3). «Не говорить, что AI» против
   `honesty_mode: honest`; «Джарвис» против имени персоны; обещание за третье
   лицо — законная формулировка или дефект. Пайплайн обязан ПОКАЗАТЬ конфликт;
   выбор — владельца, и цена ошибки лежит на нём, а не на коде.
3. **Логин Telegram** (S6). Код из SMS, 2FA, чужой номер. Автоматизировать
   нечего и не нужно: session-файл = полный доступ к аккаунту.
4. **Токен у BotFather** (S7). Секреты вводит владелец интерактивно; команда
   только проверяет, что токен доехал до процесса.
5. **Открытие трафика** (S15). `/funnel_gate on confirm` — команда владельца.
   Ни при каком раскладе, ни при каком флаге команда подключения его не
   выполняет: за ним стоит catch-up по непрочитанному за сутки.
6. **Пересъём бандла секретов** (S8). Пароль только через getpass, носитель —
   вне машины. Автомат не может ни ввести пароль, ни унести файл.
7. **Согласие клиента на доступ к аккаунту** (S5). Разговор с человеком и его
   слово. Автомат может только требовать факт и отказываться идти дальше без
   него — что и делает.
8. **Разрешение на трату** (S13). Не потому, что суммы велики, а потому, что
   человек обязан видеть списание до него, а не узнавать от бота.

Пункты 1–4 названы заказчиком; 5–8 добавлены по той же причине — чтобы через
месяц никто не пытался их «доделать».

---

## 6. Обрыв посреди подключения

Обрыв обязан быть невидим для результата. Требования, из которых это следует:

* **Каждая запись атомарна.** `registry.yaml` пишется во временный файл рядом
  и переименовывается поверх; половины файла не существует ни в один момент.
  Реестр читает гардиан каждые ~30 с — полуфайл означал бы `fatal` у всех
  клиентов сразу, включая живых.
* **Ни один шаг не зависит от предыдущего в памяти.** Между шагами процесс
  может умереть; следующий запуск читает те же факты.
* **Порядок выбран так, что любое прерывание безопасно.** Опасное состояние
  ровно одно — «в реестре `enabled: true`, а конфига/сессии нет»: гардиан
  начнёт цикл подъёма. Поэтому включение (S11) стоит ПОСЛЕ конфига (S4) и
  сессии (S6), и оно же — последнее автоматическое действие перед приёмкой.
  Это тот самый порядок деплоя, из-за которого сегодня трижды понадобились
  ворота.
* **Платное не повторяется** (S13). Прерванный дрил оставляет файл прогресса,
  и он читается: харнесс пишет `state/drills/<ts>.md` ПОСЛЕ КАЖДОГО ШАГА
  именно на этот случай.

---

## 7. Сторожа

Пишутся **от этой спеки**, автором, который планового кода не видел
([[jarvis-guards-not-by-the-plan-author]]). Ниже — что ловить, а не как.

| # | что ловит |
|---|---|
| Д1 | обрыв на КАЖДОМ шаге: процесс убит после шага N, повторный запуск доводит до конца и не повторяет ни одного действия |
| Д2 | порядок: при отсутствии валидного конфига или сессии `enabled: true` не выставляется НИ ПРИ КАКОМ пути |
| Д3 | деньги: при существующем результате дрила второй прогон не запускается без явного флага |
| Д4 | радиус: отказ замера catch-up останавливает подъём; `-Force` не подставляется автоматически ни в одной ветке |
| Д5 | токен: общее (не пер-клиентское) имя env-переменной или совпадение с включённым клиентом → останов |
| Д6 | allowlist: произвольный id не вписывается; дрил-контакт берётся из `payments/drill_gate.DRILL_CONTACTS`; owner id обязателен |
| Д7 | состояние ТОЛЬКО из фактов: удаление журнала не меняет ни одного вердикта; подделка журнала — тоже |
| Д8 | живой клиент не трогаем: slug уже `alive` с открытым гейтом → команда отказывается целиком, а не «доподключает» |
| Д9 | запись реестра атомарна: прерывание в момент записи не оставляет файла, который `parse_registry` не читает |
| Д10 | приёмка живости не принимает `starting` за `alive` и не принимает протухший heartbeat |
| Д11 | у КАЖДОЙ остановки непустое «что сделать» — структурно, по всем ветвям, а не на трёх примерах |
| Д12 | идемпотентность: два запуска подряд на завершённом подключении не меняют на диске ничего |
| Д13 | `active.yaml` не изменяется ни на одном шаге (§0-бис) |
| Д14 | `funnel_gate` не переводится в `true` ни на одном шаге и ни при каком флаге |
| Д15 | согласие: без `CONSENT.md` подключение не доходит до логина; пустой файл и файл без даты — тоже останов |
| Д16 | маркер бандла: маркер без слага этого клиента → останов; вердикт НЕ зависит от mtime сессии (двигать сессию — вердикт не менять) |
| Д17 | деньги: без `--drill-yes` дрил не запускается НИ РАЗУ; флаг не запоминается между запусками |

Д1 и Д7 — главные: они держат то самое требование, ради которого арка и
затевается. Д14 — единственный, чья цена измеряется в живых лидах.

---

## 8. Мутационный гейт

`scripts/mutate_connect_pipeline.py`. Мишени — механика ворот, а не тексты:
снятая проверка порядка (S11 до S4), `alive` из `starting`, повторный платный
дрил, чтение состояния из журнала, подстановка `-Force`, пропуск проверки
уникальности env-имени токена. Каждая мутация обязана краснеть РОВНО у того
сторожа, который на неё заведён, и гейт обязан отдавать rc 1.

Перед каждым прогоном — статическая сверка мишеней с кодом
([[jarvis-mutation-gate-target-drifts-from-code]]): сегодня она нашла две
уехавшие мишени за секунды.

---

## 9. Приёмка арки

Единственная честная — **живое подключение**, но не на живом клиенте:

1. **Репетиция в песочнице.** Отдельный root (`C:\temp\connect_rehearsal`),
   слаг-однодневка, FakeLLM, дрил-контакт. Прогон целиком, с тремя
   искусственными обрывами (после S4, S10, посреди S13) и продолжением той же
   командой. Живого реестра и живых сессий не касается вовсе.
2. **Живое подключение следующего настоящего клиента** — с владельцем рядом,
   по одной команде, с фиксацией: сколько раз она останавливалась, на чём, и
   было ли понятно из текста, что делать. Это и есть предмет арки: не «код
   работает», а «человек прошёл путь, ни разу не заглянув в мануал».
3. `docs/chatter/ONBOARDING_MANUAL.md` после этого получает шапку: ручной путь
   остаётся картой, но подключение идёт командой. Расхождение мануала с кодом
   — тот же класс, что сегодняшняя AST-сверка одобренного текста.

---

## 10. Риски

* **DEV-36 открыт:** C7 автоприёмки может позеленеть на чужом файле по
  совпадению имени клиента. Для T7 это значит: S1 не имеет права быть
  единственным доказательством, что конфиг собран ИМЕННО для этого слага.
  Проба S4 сверяет `slug` внутри конфига, а не только путь.
* **Замер радиуса слеп к сообщениям, пришедшим при лежащем раннере** — записано
  в его же докстринге и подтверждено живьём 17.08. Для нового клиента это
  неважно (БД пуста), для переподключения — важно; в §3 шаг S10 не обещает
  большего, чем замер умеет.
* **`drill_reset` не чистит деньги** (`quotes`/`invoices`/`payments` переживают
  сброс). Первый дрил на новом клиенте это не задевает — БД пуста, — но если
  когда-нибудь появится «переподключить», это ворота, которых сейчас нет.
* **Стоимость арки.** Шесть-восемь пар агентов по образцу T1–T6. Это не мелкая
  правка; если нужен более дешёвый заход — §11 q4.

---

## 11. Вопросы к владельцу — ✅ ЗАКРЫТЫ 20.08

Все шесть отвечены, ответы вынесены в шапку и разнесены по разделам:
q1 → §0-бис и Д13 · q2 → §4 (четыре кода) · q3 → S8 и Д16 ·
q4 → §4 `--drill-yes`, §5.8 и Д17 · q5 → §12 (отдельный модуль) ·
q6 → S5, §5.7 и Д15.

Открытых вопросов нет. Дальше — §12 и код.


---

## 12. КОНТРАКТ МОДУЛЯ (обязателен для обеих сторон)

Сторожа и плановый код пишут РАЗНЫЕ авторы, друг друга не видящие
([[jarvis-guards-not-by-the-plan-author]]). Значит имена и формы данных обязаны
быть заданы ЗДЕСЬ, иначе сторожа проверят фантом, а код окажется правильным
про другое. Всё, чего в этом разделе нет, автор решает сам — и сторож не имеет
права на это опираться.

### 12.1. Состав

```
chatter/connect/
  __init__.py
  model.py       Verdict, Owner, StepResult, Step, Ctx — данные, БЕЗ ввода-вывода
  probes.py      probe_s0 … probe_s15 — ТОЛЬКО чтение фактов, ничего не меняют
  actions.py     act_s4, act_s9, act_s10, act_s11, act_s12, act_s13 — ровно шесть автошагов
  steps.py       STEPS — литеральный кортеж в порядке исполнения
  __main__.py    CLI, порядок, коды выхода, тексты остановок
```

Модуль **отдельный от `chatter.onboard`** (решение q5). Импорта `chatter.onboard`
из `chatter.connect` нет: сборка файлов и подъём живого клиента — разные права.

### 12.2. Данные

```python
class Verdict(str, enum.Enum):
    CLOSED   = "закрыт"
    OPEN     = "не закрыт"
    CONFLICT = "противоречие"

class Owner(str, enum.Enum):
    AUTO  = "автомат"
    HUMAN = "человек"

@dataclass(frozen=True)
class StepResult:
    step_id: str          # "S6"
    verdict: Verdict
    why: str              # одна строка: какой факт есть/отсутствует И чем это грозит
    todo: str             # что сделать; НЕПУСТО у любого не-CLOSED (Д11)
    facts: dict           # машиночитаемые улики: пути, признаки. СЕКРЕТОВ НЕТ НИКОГДА

@dataclass(frozen=True)
class Step:
    id: str               # "S0".."S15"
    title: str
    owner: Owner
    probe: Callable[[Ctx], StepResult]
    act: Callable[[Ctx], StepResult] | None   # None у HUMAN-шагов; AUTO <=> act is not None

@dataclass(frozen=True)
class Ctx:
    root: Path
    slug: str
    now: float
    env: Mapping[str, str]
    run: CommandRunner       # ЕДИНСТВЕННЫЙ путь наружу: подпроцессы только через него
    drill_yes: bool
    drill_again: bool
```

`facts` — единственное, что читают сторожа для тонких утверждений; `why`/`todo`
проверяются на непустоту и адресность, но НЕ на дословный текст: сторож на
буквы сообщения превратил бы правку формулировки в красное.

`CommandRunner` — протокол с одним методом
`run(argv: list[str], *, cwd: Path, timeout: float) -> CommandResult(rc, stdout, stderr)`.
Внешние вызовы (`registry_cli`, `chatter_client.ps1`, `drill_runner.py`,
`reencrypt_env.ps1`) идут ТОЛЬКО через него. Прямой `subprocess` в `probes.py`
и `actions.py` запрещён — это и делает сторожей возможными без живых процессов.

### 12.3. Порядок исполнения (единственный)

```
для каждого шага в STEPS:
    r = probe(ctx)
    если r.verdict == CONFLICT:                     -> печать, выход 1
    если r.verdict == CLOSED:                       -> следующий шаг
    если owner == HUMAN:                            -> печать, выход 3
    если owner == AUTO:
        act(ctx)
        r2 = probe(ctx)                             # ДОКАЗАТЕЛЬСТВО, а не «act не упал»
        если r2.verdict != CLOSED:                  -> печать, выход 1
все шаги закрыты (кроме S14/S15, они HUMAN и последние) -> выход 0
```

Повторная проба после действия обязательна: «команда отработала» и «факт
появился» — разные утверждения, и сегодня это стоило бы панели.

### 12.4. Тексты остановки

Ровно три строки, в этом порядке и с этими префиксами:

```
ОСТАНОВ на <id>: <title>.
ПОЧЕМУ: <why>
ЧТО СДЕЛАТЬ: <todo>; после этого повтори ту же команду.
```

### 12.5. Красные линии кода

* `probes.py` НИЧЕГО не пишет на диск — ни файла, ни строки журнала;
* `actions.py` пишет только то, что назван писать шаг; запись `registry.yaml` —
  через временный файл и `os.replace`, никаких частичных состояний;
* ни один модуль не пишет в `chatter/clients/active.yaml` (Д13) и не ставит
  `funnel_gate: true` (Д14);
* `-Force` у `chatter_client.ps1` не подставляется никогда (Д4);
* секреты не попадают ни в `facts`, ни в журнал, ни в вывод: доказываем
  ПРИЗНАКОМ («переменная непуста, длина 46»), а не значением.

### 12.6. Дозакрыто по замечаниям первого автора (20.08)

Пять мест, где контракт недоговаривал. Сторож и код сошлись бы на них только
случайно — а «сойтись случайно» и значит разъехаться на первой правке.

**1. Идентификаторы шагов берутся ТОЛЬКО из таблицы §3.** Проза §4–§6 после
вставки S5 (согласие) какое-то время ссылалась на старые номера; расхождение
найдено первым автором и исправлено. Правило на будущее: номер шага живёт в
одном месте — в таблице; если он понадобился в прозе, он туда КОПИРУЕТСЯ, и
это копия со всеми свойствами копии ([[jarvis-two-numbers-for-one-thing]]).

**2. Ключи `facts` — минимальная схема.** Сторожа читают `facts`, значит имена
обязаны быть заданы, а не выдуманы каждым автором:

| шаг | обязательные ключи |
|---|---|
| S0 | `env_missing: list[str]`, `registry_fatal: str \| None` |
| S1 | `report_path: str`, `red: list[str]`, `flags: list[str]` |
| S2 | `mark_path: str`, `mark_present: bool` |
| S3 | `flags_named: list[str]`, `flags_missing: list[str]` |
| S4 | `client_dir: str`, `loads: bool`, `slug_in_config: str \| None` |
| S5 | `consent_path: str`, `consent_date: str \| None` |
| S6 | `session_path: str`, `session_available: bool` |
| S7 | `env_name: str`, `token_len: int`, `env_enc_state: str` |
| S8 | `marker_path: str`, `slug_named: bool` |
| S9 | `allowlist: list[int]`, `owner_present: bool`, `drill_present: bool` |
| S10 | `entry_present: bool`, `runnable: bool`, `error: str \| None` |
| S11 | `enabled: bool`, `radius_ok: bool` |
| S12 | `state: str`, `heartbeat_age: float \| None`, `isolated_token_line: bool` |
| S13 | `run_path: str \| None`, `rc: int \| None`, `estimate_usd: float` |

Ключей может быть больше; перечисленные обязаны быть. `token_len` вместо
значения — иллюстрация правила §12.5, а не исключение из него.

**3. `CommandRunner` при беде.** Таймаут, ненайденный исполняемый, любая
невозможность выполнить — это **исключение**, а не `CommandResult` с выдуманным
`rc`. Ловит его вызывающая проба или действие и превращает в `CONFLICT` с
внятным `why`: «замер не состоялся» и «замер сказал нет» обязаны различаться
(тот же принцип, что у отказа замера радиуса в §2.3).

**4. Журнал пишет `__main__.py`, и только он.** `probes.py` не пишет ничего,
`actions.py` пишет ровно то, что назван писать шаг. Журнал остаётся тем, чем
объявлен в §1: чтением для человека, которое код не читает никогда.

**5. Нарушение контракта — это код 2, а не трассировка.** `ConnectContractError`
(и любое исключение сборки шагов) CLI обязан поймать и напечатать как «прогон
НЕ состоялся: контракт нарушен, <что именно>». Трассировка в лицо человеку,
пришедшему подключать клиента, — это отказ, который ничего не объясняет.

**6. `Ctx.now` — unix-секунды (float).** `act_s12` — ограниченное по времени
ожидание с повтором пробы, **без записи на диск**: у приёмки живости нет своего
действия, кроме терпения.

### 12.7. Дозакрыто по замечаниям третьего сторожа (20.08)

Сторож нашёл **дыру в алгоритме §12.3**, а не в чужом коде. Записываю решения.

**1. 🔴 У автошага не было ветки «жду разрешения человека».** По §12.3 отказ
`act` даёт `r2 != CLOSED` → код 1 «ПРОТИВОРЕЧИЕ». Но денежные ворота S13 — это
именно ожидание человека (§5.8), и они выдавали бы «сломано» на штатном пути.
Ровно то, чего владелец велел избежать решением q2.

Правка: `StepResult` получает поле **`waits_for_human: bool = False`**.
Алгоритм §12.3 дополняется:

```
если owner == AUTO:
    act(ctx)
    r2 = probe(ctx)
    если r2.verdict == CLOSED:                      -> следующий шаг
    если r2.waits_for_human или act вернул waits_for_human -> печать, выход 3
    иначе                                            -> печать, выход 1
```

**Старшинство, чтобы не было второй трактовки:** `waits_for_human` значим
ТОЛЬКО при вердикте, отличном от `CLOSED`. Факт на диске старше ожидания —
если шаг закрыт, ждать нечего, и алгоритм идёт дальше, что бы ни стояло в
булевом поле. Автор `model.py` вправе требовать непустой `todo` и в этой
комбинации (защита от забывчивости), но на порядок §12.3 это не влияет.

Одно булево, а не четвёртый вердикт: «ждёт человека» — это не другое состояние
факта, а другой АДРЕСАТ следующего действия. `act_s13` без `--drill-yes`
возвращает `waits_for_human=True`.

**2. 🔴 `state/drills/<ts>.md` не содержит слага — чужой прогон закрыл бы S13.**
На машине уже лежат дрилы других клиентов; проба увидела бы чужой файл и
объявила дрил состоявшимся, не потратив ни цента и ничего не проверив. Это
ложный зелёный на шаге, который единственный доказывает, что бот отвечает.

Правка, обе половины обязательны: `act_s13` зовёт харнесс с
`--out state/drills/<slug>`, а проба S13 дополнительно требует, чтобы в ТЕЛЕ
файла был назван этот слаг (или его дрил-контакт). Каталог отделяет, тело
доказывает.

**3. Точка входа CLI объявлена, иначе `__main__` непроверяем.**

```python
def main(argv: list[str] | None = None, *,
         runner: CommandRunner | None = None,
         root: Path | None = None) -> int
```

`runner=None` означает боевой раннер, `root=None` — корень репозитория.
Имя параметра то же, что у поля `Ctx.runner`, и это не косметика: `run` у
параметра оставляло ту же развилку «функция или объект», ради снятия которой
поле и переименовали. Одна вещь — одно имя, иначе следующий автор угадывает. Без этого
шва сторож на §12.4 и на коды выхода вынужден был бы поднимать живые
подпроцессы, то есть не существовать.

**4. Формат даты в `CONSENT.md`:** первая строка начинается с ISO-даты
`YYYY-MM-DD`, дальше свободный текст «от кого получено». Разбирается дата,
остальное — человеческое.

**5. Owner id берётся из `control.owner_chat_id` живого конфига** (у Ярины —
`control.owner_chat_id: 545893540`), не из env и не из журнала. Один источник.

**6. `env_enc_state` — перечень закрыт:** `no_env` · `no_enc` · `unreadable` ·
`in_sync` · `stale` (значения `scripts/reencrypt_env.py`). Закрывает шаг только
`in_sync`; `stale` — рабочее состояние машины, но НЕ то, из которого токен
доедет до процесса.

**7. `CommandResult` живёт в `model.py`**; `CommandRunner` — Protocol, и
`isinstance` против конкретного класса нигде не используется: подставной раннер
сторожа обязан работать по форме, а не по родству.

**8. Коллизия имени токена с ВЫКЛЮЧЕННЫМ клиентом — законна** (§2.1 говорит
«уникальное среди включённых»). Сторож на этот случай не пишется: тест,
краснеющий на более строгом коде, — плохой тест.

### 12.8. Дозакрыто по замечаниям сторожа на обрыв (20.08)

Три из них — дефекты алгоритма, а не трактовки. Один — расхождение пары
авторов, ради которого разделение и заведено.

**1. 🔴 Цикл не мог вернуть 0 НИКОГДА.** S14 (вычитка дрила) и S15 (открытие
трафика) — человеческие и не закрываются фактом на диске, значит буквальный
цикл §12.3 всегда упирался в код 3. Определение кода 0 из §4 («осталась только
команда владельца») с алгоритмом не сходилось.

Правка: **хвостовые человеческие шаги S14 и S15 в цикл не входят.** Цикл идёт
по S0…S13; когда все они CLOSED, команда печатает то, что осталось за
человеком (вычитать текст дрила, открыть трафик отдельной командой) и выходит
с кодом 0. Это ровно то, что §4 и обещал: «подключение доведено, дальше слово
владельца».

**2. 🔴 Полукопия каталога заклинивала подключение навсегда.** S4 копировал
файл за файлом; обрыв посреди копирования оставлял каталог, который не грузится,
и следующий запуск объявлял ПРОТИВОРЕЧИЕ — то есть команда не могла починить
то, что сама же и оставила.

Правка, того же класса, что атомарная запись реестра: **`act_s4` собирает копию
во временный каталог рядом и въезжает одним переименованием.** Полукопии от
нашей руки не существует. Отсюда вердикты S4 становятся однозначными:

* каталога нет → НЕ ЗАКРЫТ, действие копирует;
* каталог есть и грузится `load_config` → ЗАКРЫТ;
* каталог есть и НЕ грузится → ПРОТИВОРЕЧИЕ. Наша рука такого не оставляет,
  значит это правка человека, и разбирать её человеку: там может лежать
  вычитанный тон, который перезаписывать нельзя ни при каких условиях.

**3. 🔴 Согласие лежало внутри каталога, который создаёт S4 — тупик.**
`CONSENT.md` пишет человек ДО логина (S5), а каталог клиента появляется на S4.
Человек, положивший согласие первым, создал бы каталог с одним файлом — и S4
увидел бы «есть, не грузится» → ПРОТИВОРЕЧИЕ. Подключение вставало бы намертво
у того, кто всё сделал правильно.

Правка: согласие живёт **`state/connect/<slug>.consent.md`**, рядом с маркером
бандла, и с каталогом клиента не пересекается вовсе. Цена решения названа:
`state/` не в git, поэтому запись согласия обязана попадать в регулярный бэкап
состояния (задача `JarvisStateBackup`), иначе единственная улика доступа к
чужой переписке живёт в одном экземпляре.

**4. Поле контекста переименовано: `Ctx.run` → `Ctx.runner`.** Один автор звал
`ctx.run(argv, ...)`, другой ждал `ctx.run.run(argv, ...)` — обе трактовки
законны при нынешнем имени, а `ctx.run.run` вдобавок читается как опечатка.
Форма одна: `ctx.runner.run(argv, cwd=..., timeout=...)`.

**5. Атомарность касается НЕ ТОЛЬКО реестра.** `act_s9` правит `settings.yaml`
ЖИВОГО клиента — тот же класс обрыва: полуфайл конфига читает раннер и гардиан.
Правило распространяется: любая наша запись в существующий файл конфигурации
идёт через временный файл рядом и `os.replace`.

**6. Нужны две AST-сверки, которых сегодня нет ни у кого:**
* в `probes.py` и `actions.py` нет прямого `subprocess` — иначе шов §12.2
  обходится, и сторожа перестают что-либо доказывать, оставаясь зелёными;
* `chatter.connect` не импортирует `chatter.onboard` (решение владельца q5:
  разные права и разный радиус ошибки; слитый модуль однажды подключит
  что-нибудь при сборке).

**7. Принято к сведению без правки:** «красных проверок нет» на S1 читается по
`report.json` того формата, что отдаёт `chatter/onboard/report.py`; поле там
называется иначе, чем в прозе §3, и автор проб обязан взять реальное имя из
кода, а не из спеки.

### 12.9. Дозакрыто по замечаниям автора проб (20.08)

**1. 🔴 Моя схема §12.6 врала про флаги.** Я написал «подмножество C12/C13/C16»,
опираясь на `FLAG_IDS`. Факт: `checks._c7` ставит `is_flag=True` РУКОЙ, и C7
штатно попадает в блок флагов отчёта. Значит S3 требует назвать **ВСЕ поднятые
в `report.json` флаги**, а не только members `FLAG_IDS`. Иначе прощался бы ровно
тот флаг, который говорит «сценарий нашли по совпадению имени» — DEV-36 из §10.

**2. S1 не может проверить «красных нет» по `report.json`:** вердиктов C1–C17
там нет вовсе, отчёт хранит флаги, счётчики и разделы. Прогонять `--check`
внутри S1 нельзя — его rc по решению q3 уже содержит метку вычитки, то есть S2.
Принято: `red` на S1 — это то красное, которое отчёт доказывает САМ
(`slug_mismatch`, `file_missing`); красное автоприёмки живёт в S2. Отдельной
правкой в `chatter.onboard` мы это не чиним: там своя граница.

**3. `Config.slug` для S4 не годится** — он равен переданному аргументу и
совпадёт всегда, то есть проверка DEV-36 была бы зелёной по построению.
Принадлежность доказывается содержимым: `control.control_bot_token_env`
(`CHATTER_CONTROL_BOT_TOKEN_<SLUG>`), иначе шапка `settings.yaml`. Не нашлось —
шаг закрывается, но `why` прямо говорит, что принадлежность доказана только
путём: legacy-конфиги иначе краснели бы навсегда.

**4. `radius_ok` не меряется своей копией:** `chatter_client.ps1` выставляет
`enabled: true` ТОЛЬКО после чистого замера, поэтому `radius_ok = enabled`.
Своя проба радиуса вдобавок ломала бы идемпотентность — у работающего клиента
диалоги с последним словом лида появляются штатно.

**5. Смету считает САМ ХАРНЕСС, а не мы.** Ставки `COST_TURN_COLD/WARM` живут в
`scripts/drill_runner.py`; наша копия ставок и правила «холодный ход» была бы
вторым определением одной вещи ([[jarvis-two-numbers-for-one-thing]]) и разошлась
бы молча. `act_s13` без разрешения зовёт харнесс в режиме плана (без `--yes`),
берёт смету из его вывода и печатает её человеку.

**6. 🔴 Отчёт дрила не называет, ЧЕЙ он.** `format_report` печатает только
`# Дрил: <name>`, хотя в сценарии есть `client` и `contact`. Требование §12.7
п.2 сегодня невыполнимо, и это дефект не нашей арки, а харнесса: отчёт обязан
отвечать на вопрос «чей прогон», иначе на машине с несколькими клиентами он
улика ни о чём. Правка отдельной задачей: в шапку отчёта добавляется строка с
`client` и `contact`. До неё проба S13 обязана краснеть (`CONFLICT`), а не
считать чужой отчёт своим — так и сделано.

**7. Импорт `chatter.onboard` — узкое исключение.** Запрет из q5 про ПРАВА и
радиус ошибки, а не про две замороженные константы. Разрешено:
`from chatter.onboard.checks import REVIEWED_FILENAME` и такие же константы.
Запрещено всё, что ЗАПУСКАЕТ пайплайн (сборку, рендер, пересборку конфига).
AST-сверка §12.8 п.6 пишется с этим исключением, а не против него.

**8. Пробы не читают `ctx.drill_yes`/`drill_again`** — флаг это разрешение
действовать, а не факт на диске. Следствие: `--drill-again` при уже зелёном S13
отрабатывает в `__main__`, а не через вердикт пробы.

### 12.10. Дозакрыто по замечаниям сторожа CLI (20.08)

**1. КАК именно исключаются хвостовые шаги.** §12.8 п.1 сказал «S14/S15 в цикл
не входят», но не сказал механизм — а от механизма зависят три файла. Решение:
в `STEPS` они ЕСТЬ (карта обязана быть полной: это то, что человек читает в
`--plan`), а исключение живёт отдельной именованной константой в `steps.py`:

```python
TAIL_HUMAN_IDS = ("S14", "S15")   # закрываются словом владельца, не фактом
```

Цикл идёт по `STEPS`, пропуская `TAIL_HUMAN_IDS`; когда всё остальное CLOSED,
CLI печатает их как «осталось за человеком» и выходит с 0. Константа
литеральная и одна: вывести её из `owner == HUMAN` нельзя — человеческих шагов
десять, а хвостовых ровно два.

**2. Код выхода у `--plan`:** `0`, если карта построена (какими бы ни были
вердикты — `--plan` отвечает на вопрос «где я», а не «всё ли хорошо»), и `2`,
если факты прочитать не удалось. Кодов 1 и 3 у `--plan` не бывает: он ничего не
делает, значит ни ждать, ни ломать не может.

**3. Журнал:** ДОПИСЫВАЕТСЯ (append), одна запись на ИСПОЛНЕННЫЙ шаг. Прогон,
который ничего не исполнил, журнала не заводит и не трогает — иначе повторный
запрос состояния менял бы диск, а §12 обещает обратное. Формат человеческий,
машина его не читает (§1).

**4. Ветка кода 2 три строки §12.4 не держит** — в ней нет шага, который
назвать. Требование к ней одно: сказано, ЧТО именно нарушено, и трассировки
нет.

---

## 13. ПЕРЕД МЕРЖЕМ: свести §12.6–§12.10 в тело спеки

Эти пять разделов — журнал правок, а не спека. Они появились потому, что семь
авторов читали документ параллельно и находили в нём дыры быстрее, чем я успевал
переписывать тело. Для работы это правильный порядок, для чтения — нет:
следующий человек не обязан реконструировать решение из пяти поправок к одному
абзацу.

Поэтому ПЕРЕД мержем: решения переносятся в §1–§12, разделы 12.6–12.10
схлопываются в короткий список «что менялось по ходу и почему», а таблица §3 и
контракт §12 остаются единственным местом, где написано, как оно устроено.
Это не косметика: спека — один из трёх экземпляров правды об этом коде, и
расхождение с ним ловится ровно так же, как расхождение сторожа с кодом
([[jarvis-approved-text-three-places]]).

### 12.11. Дозакрыто по замечаниям автора действий (20.08)

**1. 🔴 Дрил — СУФЛЁР, и через захваченный stdout человек не увидел бы ничего.**
`drill_runner` печатает реплики человеку, идущему к телефону, ПО ХОДУ прогона;
`CommandRunner` вывод захватывает. Шаг, задуманный автоматическим, тихо
превратился бы в «сиди и жди, подсказки увидишь потом».

Решение: **S13 идёт в автономном режиме** — `--auto-lead`, реплики шлёт процесс
лида (`.secrets/drill_lead.session`), человек у телефона не нужен; харнесс сам
снимает часовой таймер первого шага, заведённый ради идущего к телефону.
Автономный прогон уже доказан живьём 06.08: пять шагов из пяти без человека.

Если предпосылок автолида нет (сессия лида недоступна, аккаунт персоны не
резолвится в `--lead-peer`), S13 **становится человеческим шагом**: печатается
точная команда, которую человек запускает в СВОЕЙ консоли, где суфлёр виден.
Тихого прогона с невидимыми подсказками не бывает ни в одной ветке.

**2. §2.2 требовал недостижимого для S10.** «`runnable=true, error=null`» —
валидация реестра смотрит только ВКЛЮЧЁННЫХ, а S10 пишет `enabled: false`.
Уточнение: доказательство S10 — запись есть и валидация **на включённой КОПИИ
записи** не даёт ошибок. Так и сделано в пробе.

**3. Сценарий дрила живёт в каталоге клиента**, куда его переносит `act_s4`
вместе с конфигом. Действие берёт путь из фактов пробы и отказывается платить,
если сценария нет там, где проба его читает: иначе деньги списаны, а шаг открыт.

**4. Посторонние id в allowlist СОХРАНЯЮТСЯ и называются вслух.** Стереть
чужую строку значит выключить бота живому человеку. Действие их не трогает,
проба не считает дефектом, оба говорят о них в отчёте.

**5. `--root` добавлен в §4** — он нужен репетиции §9.1 в отдельном корне и не
является флагом «сделай шаг N». У `chatter_client.ps1` дефолт `C:\jarvis`,
поэтому в его argv `-Root` передаётся ВСЕГДА: без этого репетиция в песочнице
правила бы живой реестр.

**6. Временная поддержка старого имени `Ctx.run` снимается** после правки
`model.py`. Два имени одного поля — та же болезнь, что чинилась
переименованием; жить ей ровно до синхронизации авторов.

### 12.12. Дозакрыто по замечаниям сторожа на порядок (20.08)

Два пункта — НАСТОЯЩИЕ дефекты планового кода, найденные сторожем, который
этого кода не видел. Ради этого дисциплина и держится.

**1. 🔴 Д8 не был реализован: команда «доподключала» ЖИВОГО клиента.** Замер
сторожа: клиент `alive`, heartbeat свежий, `funnel_gate` ОТКРЫТ, allowlist
пуст — команда выполнила `act_s9` (переписала `settings.yaml` живого клиента) и
дошла до S13, то есть до платного дрила в БД клиента с открытой воронкой.

Уточнение спеки, которого не хватало: **Д8 не привязан к шагу**, поэтому его
негде было исполнить. Привязка: проверка «этот слаг уже живой» стоит **до
первого действия**, то есть не позже S9, и её отказ — код 1 (ПРОТИВОРЕЧИЕ), а
не 3: подключать уже подключённого нечего, это не ожидание.

**2. 🔴 Улика утверждала то, чего не проверяли.** `probe_s12` коротит на
не-`alive` и отдаёт `isolated_token_line: False` — при СУЩЕСТВУЮЩЕЙ строке в
логе. Человек по такой улике пойдёт перешифровывать исправный токен.

Правило, общее для всех проб: **`False` значит «проверили, нет»; «не
проверяли» — это `None`.** Ключ, который проба не смотрела на своей ветке,
обязан быть `None`, а не значением по умолчанию. Схема §12.6 п.2 читается с
этим уточнением.

**3. Незнакомое `env_enc_state` — ПРОТИВОРЕЧИЕ, а не «не закрыт».** Перечень
закрыт пятью значениями (§12.9 п.6), а код уже отдал шестое (`unknown`).
Значение вне алфавита означает, что мы не поняли ответ инструмента, — это
«факты спорят», а не «шаг предстоит».

**4. Атомарность `act_s4` не закрыта ничем.** Класс тот же, что у Д9 для
реестра: полукопии каталога не бывает. Сторож заводится тем же автором, что
писал Д9, — у него уже есть механика перехвата подмены.

**5. 🔴 ПРОВЕРЕНО: бэкап согласие НЕ забирает.** `app/services/state_backup.py`
держит ЯВНЫЙ белый список `CRITICAL_PATTERNS` («anything NOT listed here is
never backed up»), и `connect/` в нём нет. То есть единственная улика доступа
к чужой личной переписке жила бы в одном экземпляре на одном диске.

Правка отдельной задачей: в белый список добавляются `connect/*.consent.md` и
`connect/*.bundle.txt` (маркер бандла — свидетельство того же класса). Список
намеренно белый, поэтому расширение — это код и сторож, а не примечание.
