# Keep-alive кэша классификатора и brain — СПЕКА (проектирование)

Статус: **АРКА ПРИНЯТА К ПОСТРОЙКЕ (решение владельца 19.08.2026).**
Очередь: после пер-клиентного сторожа и Хайку на классификаторе (порядок 4 → 1 → 2 → 3).
Автор-сессия: автономный прогон 2026-08-09, журнал `docs/sessions/2026-08-09-autorun.md`.

> ### ⚠️ Отмена арки ОТМЕНЕНА — читать до всех расчётов ниже
>
> Между 10.08 и 19.08 арка была закрыта с вердиктом «в минусе ×4». **Вердикт
> снят: он посчитан на объёме Ольги — 16 диалогов/мес.** Ольга — тестовый
> прототип, а не клиент.
>
> **Поправка 19.08:** тогдашний безубыток «61 диалог/мес» сам был занижен —
> §4 считал стоимость по двум префиксам, а экономию по одному. Настоящий
> безубыток при окне 12ч/сут — **13.2 диалога/мес**, то есть Ольга с её 16
> диалогами была В ПЛЮСЕ, и вердикт «в минусе ×4» неверен ДВАЖДЫ: и по
> объёму, и арифметически. Разбор ошибки — в §4.
>
> Реальные клиенты (встречи 20–21.08, ~6 компаний): **300–600 диал/мес**,
> у части до 1000 коротких. Против безубытка 61 это **окупаемость ×5–×10**.
>
> **Класс ошибки:** экономику арки мерили на объёме, который на порядок ниже
> порога её собственной окупаемости, названного в этой же спеке. Число было
> верным, вывод из него — нет.
>
> Сохранены и НЕ пересчитываются: замер Г0 (24.1% холодных), гейты Г1 и Г2.
Предшественник: `2026-07-25-chatter-classifier-cache.md` (арка кэша, закрыта, `4ae17a73`).

---

## 0. Зачем и на каком факте

Арка кэша 25.07 снизила стоимость вызова классификатора на −61%, но оставила
хвост: **холодные ходы**. Ход после разрыва ≥ TTL платит запись по ставке 1h
($6/M) вместо чтения ($0.30/M) — разница $0.0565 против $0.0125 на вызов.

Замер модели предполагал 21% холодных. **Факт из `llm_usage` за 7 дней
(03.08–09.08, 37 вызовов классификатора): 18.9%.** Экономия keep-alive
пересчитывается с $83 до $75 на 1000 диалогов/мес, то есть −10%: порядок
величины модель угадала, решение не переворачивается.

⚠️ **Оговорка о репрезентативности.** Окно замера почти целиком состоит из
`JarvisDrillNightly` (таск в 10:00, ровно 6 вызовов подряд с шагом 1–2 мин).
Живого трафика в нём нет, и 18.9% ≈ 1/6 — это кадэнс дрила, а не поведение
лидов. Совпадение с 21% случайное. Цифру необходимо переснять на живом
трафике до приёмки (см. §6, гейт Г0).

**Факт, который сильнее самой доли и на котором стоит вся арка:**

| период | вызовов | холодных | из них TTL истёк | из них сломан префикс |
|---|---:|---:|---:|---:|
| до фикса кэша (≤25.07 21:00) | 36 | 26 (72.2%) | 7 | 19 |
| **после фикса (>25.07)** | **62** | **16 (25.8%)** | **16** | **0** |

После фикса **все 16 промахов из 16 — истёкший TTL**, поломок префикса ноль.
Keep-alive адресует 100% текущих промахов. Если бы часть промахов была от
поломки префикса, keep-alive лечил бы симптом — здесь не так.

---

## 1. Что пингуем

**Не «кэш клиента», а каждый кэшируемый ПРЕФИКС.** Их два на клиента, и это
первое, что легко упустить:

| тег | что в префиксе | размер (volska, замер 19.08) |
|---|---|---|
| `classifier` | `classifier_stable_prefix(playbook, language, …)` | 7 844 ток |
| `brain` | системный промпт персоны (персона+знания+плейбук+правила+примеры) | 8 586 ток |

⚠️ Числа ОБНОВЛЕНЫ 19.08: срезка плейбука (а) вырезала секцию ключевых слов
эскалации из текста, уходящего в модель (спека `2026-08-19-playbook-trim.md`).
Прежние 8 384 / 9 127 описывают несуществующий больше префикс — не использовать.
Размеры РАЗНЫЕ у разных клиентов (yarina: brain 13 869, классификатор 8 039),
поэтому стоимость режима считается пер-клиент, а не по volska.

Оба идут через `AnthropicLLM.complete` с одним брейкпоинтом
`cache_control: {ephemeral, ttl: 1h}` на первом system-блоке; изменчивое
(`uncached_suffix`) — после брейкпоинта (контракт арки кэша, закреплён тестом).

Плейбук и персона **свои у каждого клиента** → префиксы разные → кэш-записи
разные → **пингов нужно 2 × (число клиентов)**. Это не одна фоновая задача.

**Не пингуем:** `classifier_retry` (тот же префикс, что `classifier` — отдельная
запись не создаётся), и любые вызовы без `cache_control`.

---

## 2. Чем пингуем

**Механизм: `max_tokens: 0`** — прогрев/поддержание кэша штатным средством API.
Запрос выполняет prefill (то есть касается кэш-записи и продлевает TTL) и
возвращается сразу: `content: []`, `stop_reason: "max_tokens"`, выходные токены
не тарифицируются.

Форма — **точная копия боевого запроса до брейкпоинта**, иначе это будет другой
префикс и пинг прогреет не тот кэш:

```
model            = тот же
system[0]        = тот же байт-в-байт + cache_control {ephemeral, ttl: 1h}
system[1]        = НЕ слать (uncached_suffix — изменчивая часть, в префикс не входит)
messages         = минимальная заглушка (единственный user-блок)
max_tokens       = 0
thinking         = как в боевом вызове
```

**Совместимость `max_tokens: 0` (проверить до реализации).** Комбинация
запрещена со `stream: true`, `thinking.type: "enabled"`, `output_config.format`,
`tool_choice` вида `{"type":"tool"}`/`{"type":"any"}` и Batches.

| тег | thinking в бою | вердикт |
|---|---|---|
| `classifier` | `{"type": "disabled"}` (`classifier.py:443`, `no_thinking=True`) | ✅ `disabled` не в списке запретов |
| `brain` | не передаётся → адаптивный дефолт модели | ✅ **ПРОВЕРЕНО ЖИВЬЁМ 2026-08-10** |

### ✅ Г1 ЗАКРЫТ ЗАМЕРОМ 2026-08-10

`max_tokens: 0` принят с боевым (адаптивным) thinking. Отказа нет:
`stop_reason=max_tokens`, `output_tokens=0` — выход не тарифицируется.
**Brain из арки не выпадает, экономика пополам не делится.**

### ✅ Г2 ЗАКРЫТ ЗАМЕРОМ 2026-08-10 — арка ЖИВА

Реальный brain-префикс volska (`sys_chars=18595`, сверен с прод-логом
09.08 10:08:53; `9127` ток совпало с боевым `cr=9127`). Сырой SDK, в `demo.db`
не писали, прод-раннер не трогали. Прод за окно замера в brain **не ходил ни
разу** — продление приписывается только пингам.

| вызов | от записи | `cache_creation` | `cache_read` | что доказывает |
|---|---|---:|---:|---|
| A | +0м00с | **9127** (1h) | 0 | Г2(б): пинг по ИСТЁКШЕЙ = запись по ставке 1h |
| B | +0м19с | **0** | **9127** | **Г2(а): пинг по ЖИВОЙ = ЧТЕНИЕ. Точка отмены пройдена** |
| C | +54м07с | 0 | 9127 | использование под конец окна |
| D | **+66м11с** | **0** | **9127** | **TTL продлевается: запись пережила исходный срок на 6м11с** |

**TTL продлевается использованием — подтверждено.** Без продления запись,
созданная в 02:10:03, умерла бы в 03:10:03, и D в 03:16:14 показал бы
`cache_creation > 0`. Показал `cache_read=9127`. Значит период пинга считается
от ПОСЛЕДНЕГО ИСПОЛЬЗОВАНИЯ, а не от момента записи — §3 остаётся в силе.

Цена прогона: **$0.0630** по прайс-листу (см. §4 об оговорке по вводной цене).
Отношение запись/чтение = ровно **20×** ($0.0548 против $0.0027), как и
предполагала спека.

---

## 3. Частота

TTL записи = 1 час, продлевается при использовании. Период пинга **50 минут**:
10 минут запаса на дрожание планировщика, сетевой ретрай и часовой сдвиг.

Меньше 50 мин — линейный рост стоимости без выигрыша. Больше 55 — риск
проскочить TTL, а проскок стоит в 20 раз дороже сэкономленного (§2, Г2).

**Пинг не нужен, когда трафик и так частый.** Живой ход сам продлевает TTL.
Правило: пинговать префикс, только если с последнего РЕАЛЬНОГО вызова этого
тега прошло > 50 мин. Дешёвый источник истины уже есть — `llm_usage.ts` по тегу.
Это убирает пинги в рабочие часы активного клиента, а именно там их было бы
больше всего.

**Окно суток — параметр клиента, не константа.** Лиды пишут не круглосуточно;
24/7 против 12ч/сут — двукратная разница в стоимости (§4).

---

## 4. Деньги: как измерить эффект

> ### 🔴 ПЕРЕСЧЁТ 19.08 — предыдущая редакция §4 ЗАНИЖАЛА выгоду ВДВОЕ
>
> **Ошибка:** стоимость пингов считалась по ДВУМ префиксам, а экономия — по
> ОДНОМУ. Раунд пинга ($0.005253) — это сумма `brain` + `classifier`, так и
> задумано (§1: «префиксов два на клиента»). А экономия выводилась формулой
> «9 ходов × 0.189 × $0.0440», где $0.0440 — дельта «запись минус чтение» для
> ОДНОГО префикса.
>
> **Почему это неверно:** каждый ход лида порождает ДВА вызова — brain пишет
> ответ, классификатор судит тот же ход, — и оба могут быть холодными.
> Проверено по боевому леджеру volska: `brain` 145 вызовов, `classifier` 140,
> то есть один к одному.
>
> **Причина ошибки, чтобы не повторилась:** «ход» и «вызов» — разные единицы,
> и в одной формуле они смешались. `9 ходов` взято из методики полос (ходов на
> диалог), а `$0.0440` — цена одного ВЫЗОВА. Множитель «×2 вызова на ход»
> потерялся на стыке двух методик. Правило: в любой формуле, где встречаются
> «ход» и «вызов», множитель между ними обязан быть НАПИСАН, даже если он
> равен единице.
>
> **Что ещё изменилось:** префиксы стали короче — срезка плейбука (а)
> вырезала секцию ключевых слов из текста для модели (спека
> `2026-08-19-playbook-trim.md`). Раунд 17 511 → **16 430** токенов.
>
> Все числа ниже — пересчитанные. Старые не восстанавливать.

**Стоимость пинга** (префиксы volska после срезки, cache read $0.30/M,
выходных токенов при `max_tokens: 0` нет):

| | ток | $/пинг |
|---|---:|---:|
| `classifier` | 7 844 | $0.002353 |
| `brain` | 8 586 | $0.002576 |
| **раунд (оба префикса одного клиента)** | **16 430** | **$0.004929** |

**Экономия на диалог.** Единицы названы явно, чтобы не повторить ошибку выше:

    9 ходов/диалог × 2 вызова/ход × (24.1% − 5%) × дельта(запись−чтение)
    = $0.1610 на диалог

Доля холодных 24.1% — живой замер Г0. Цель 5% — гейт приёмки ниже.

**Стоимость режима и безубыток на клиента:**

| окно | раундов/мес | $/мес | безубыток |
|---|---:|---:|---:|
| 24/7 | 864 | $4.26 | **26.5 диалога/мес** |
| **12ч/сут (умолчание)** | **432** | **$2.13** | **13.2 диалога/мес** |
| 8ч/сут | 288 | $1.42 | **8.8 диалога/мес** |

**Умолчание — 12 часов в сутки** (решение владельца 19.08). Мотив: безубыток
вдвое ниже, а ночной трафик у обеих боевых клиенток близок к нулю — платить за
него незачем. Окно берётся **из конфига КЛИЕНТА**, а не из константы: у клиента
с ночными лидами оно другое, и это его настройка, а не наша.

**Чистая экономия по объёмам** (окно 12ч/сут):

| диалогов/мес | было | станет (с пингами) | чисто |
|---|---:|---:|---:|
| 100 | $39.67 | $25.70 | **+$13.97 (35.2%)** |
| 300 | $119.02 | $72.85 | **+$46.17 (38.8%)** |
| 600 | $238.03 | $143.57 | **+$94.46 (39.7%)** |
| 1000 | $396.72 | $237.86 | **+$158.86 (40.0%)** |

**Вывод «keep-alive выгоден не всегда» ОСТАЁТСЯ, но порог сдвинулся вниз
вчетверо.** Экономия по-прежнему растёт с объёмом, а стоимость пингов
фиксирована на клиента; просто убыточным теперь оказывается клиент тише
13 диалогов в месяц, а не 31. Решение всё равно обязано быть по цифре
пер-клиент — интуиция здесь по-прежнему обманывает: тихий клиент это тот, у
кого БОЛЬШЕ всего холодных ходов, и именно ему арка может не окупиться.

Побочное следствие пересчёта: вердикт «арка в минусе ×4», снятый в шапке
спеки по причине «мерили на 16 диалогах Ольги», был неверен ещё и
арифметически — против настоящего безубытка 13.2 при окне 12ч/сут Ольга
находилась В ПЛЮСЕ.

**Методика замера эффекта — та же, что у арки кэша 25.07** (иначе цифры
несравнимы): ставка записи построчная из `cache_creation_1h`/`_5m`, cold-start
ходы исключены.

| метрика | откуда | гейт приёмки |
|---|---|---|
| доля холодных ходов | `llm_usage`, `cache_read == 0` по тегу | было 24.1% → стало **≤5%** |
| $/вызов классификатора | построчно по ставкам | было $0.0208 → стало **≤$0.0145** |
| стоимость пингов | отдельный тег в `llm_usage` | **≤ 15%** от сэкономленного |
| чистая экономия | экономия − пинги | **> 0** на каждом клиенте отдельно |

Последняя строка — не формальность: положительная сумма по портфелю может
скрывать убыточных тихих клиентов.

### 4.1. Приёмка Г4 идёт на VOLSKA — решение владельца 19.08

Не на `demo`. Дрил на demo даёт ИСКУССТВЕННЫЙ кадэнс, а вся арка про то, как
ведёт себя кэш в РЕАЛЬНЫХ паузах между лидами — именно этого на demo не
воспроизвести. Малый трафик volska здесь не помеха, а условие замера.

**Контроль, обязательный к закладке:** доля холодных «до» и «после» снимается
на ОДНОМ И ТОМ ЖЕ окне дней (например, 7 календарных суток до включения и 7
после), а не «сколько накопилось». Разные окна дают разный кадэнс лидов, и
сравнение поехало бы на нём, а не на пингах.

Следствие для сроков: окно приёмки — недели, а не сутки. У volska ~140
классификаторных вызовов ЗА ВСЁ ВРЕМЯ, и на сутках статистики не наберётся.
Это названо заранее, чтобы не подгонять вывод под короткое окно.

---

## 5. Что делать при промахе

Промах = пинг не попал в живую запись (`cache_read == 0` в ответе на пинг).

| причина | как отличить | действие |
|---|---|---|
| пинг опоздал (TTL истёк) | предыдущий пинг/вызов > 60 мин назад | снизить период; если повторяется — чинить планировщик, а не период |
| префикс сломан (в кэшируемый блок въехало изменчивое) | предыдущий пинг < 50 мин назад | **алерт владельцу.** Это регрессия арки кэша 23.07 — та, что прожила сутки незамеченной |
| пинг не ушёл (сеть/5xx) | исключение транспорта | ретрай 1 раз через 60с, затем пропустить цикл |

**Три жёстких правила:**

1. **Сбой пинга не трогает лида.** Keep-alive — фоновая оптимизация; его отказ
   не имеет права влиять на доставку ответа (DEV-18: логировать громко, не
   ронять).
2. **Сбои пингов НЕ считаются в `classifier_error_threshold`** (=2/сутки,
   `loader.py:91`). Иначе сетевой сбой фоновой задачи поднимет боевой алерт
   деградации классификатора и обнулит смысл порога.
3. **Пинги пишутся в `llm_usage` под ОТДЕЛЬНЫМ тегом** (`keepalive_classifier`,
   `keepalive_brain`). Под тегом `classifier` они изнутри неотличимы от боевых
   вызовов и мгновенно испортят и hit-rate, и замер §4.

### 🔴 Keep-alive ломает `cache_health.consecutive_misses`

Отдельный пункт, потому что это регрессия наблюдаемости, а не деталь.

`cache_health.consecutive_misses` обрывает цепочку промахов на разрыве ≥ TTL:

> «разрыв ≥ TTL — кэш умер законно, промах после него уликой не является»

Логика верна **только без keep-alive**. С включённым keep-alive разрыв ≥ TTL
больше не означает, что кэш умер: он должен был остаться живым. Промах после
такого разрыва — это как раз **улика**, а `is_regression` его проглотит и
промолчит. То есть арка, снижающая число промахов, одновременно ослепляет
детектор, который эти промахи ловил.

**Требование:** в той же арке `consecutive_misses` учит, что для тега под
keep-alive разрыв ≥ TTL уликой ЯВЛЯЕТСЯ. Иначе арку не принимать — иначе мы
повторяем 23.07, где кэш умер и этого никто не увидел.

---

## 6. Два и более клиента

Ядро вопроса: **у каждого клиента свой плейбук и своя персона → свой префикс →
своя кэш-запись.** Общего кэша нет и быть не может.

| что | одиночный клиент | N клиентов |
|---|---|---|
| префиксов | 2 | 2N |
| пингов/мес (12ч/сут, умолчание) | 432 | 432N |
| стоимость | $2.13 | $2.13 × N |
| безубыток | 13.2 диал/мес | 13.2 диал/мес **у каждого** |

(числа §4 после пересчёта 19.08; при окне 24/7 — $4.26 и 26.5 диал/мес)

**Что ломается при N > 1:**

1. **Стоимость линейна по клиентам, экономия — по диалогам.** Портфель из
   10 тихих клиентов: пинги $21.30/мес (окно 12ч), окупятся только при 132
   диалогах суммарно. Режим обязан включаться **пер-клиент по его объёму**, а
   не глобальным флагом.
   Глобальный тумблер здесь — это способ потерять деньги на тихих клиентах.
2. **`/reload` плейбука меняет префикс.** Кэш клиента умирает, пинги до
   следующего цикла греют мёртвую запись. Керующий цикл обязан реагировать на
   смену версии конфига (`config_versions` уже есть) — иначе после каждого
   `/reload` до 50 минут платных холостых пингов.
3. **Планировщик — общий, префиксы — разные.** 2N задач в Планировщике Windows
   не годится (см. инвентаризацию привязок к машине). Один процесс, внутри —
   список префиксов; иначе арка добавит 2N новых объектов инфраструктуры.
4. **Отказ пингов одного клиента не должен глушить остальных.** Исключение на
   клиенте A не имеет права прервать цикл клиента B.
5. **Замер §4 обязан быть пер-клиентным.** Сумма по портфелю скрывает
   убыточных.

---

## 7. Границы арки

**В арке:** механизм пинга, планирование, теги в `llm_usage`, правка
`cache_health` из §5, замер по методике §4, пер-клиентный тумблер.

**НЕ в арке:**
- Смена TTL с 1h на 5m — отдельное решение, меняет ставку записи.
- Прогрев на старте раннера — соседняя задача, другой триггер, отдельный замер.
- Оптимизация размера префикса — это арка кэша, она закрыта.

---

## 8. Порядок работ (гейты)

| # | гейт | стоимость | статус |
|---|---|---|---|
| Г0 | переснять долю холодных на ЖИВОМ трафике | $0 (чтение) | ✅ **ЗАКРЫТ.** Живой трафик: **24.1% холодных** (выше модельных 21% и выше дрилового замера 18.9% — арка выигрывает больше, а не меньше) |
| Г1 | `max_tokens: 0` + адаптивный thinking на brain | факт $0.055 | ✅ **ЗАКРЫТ 10.08** — принят |
| Г2 | пинг по живой / по истёкшей + продление TTL | факт $0.008 | ✅ **ЗАКРЫТ 10.08** — читает, TTL продлевается |
| Г3 | реализация + правка `cache_health` (§5) | — | ждёт ОК владельца |
| Г4 | замер по §4 на одном клиенте, сутки | — | чистая экономия ≤ 0 → откатить |

Г2 была точкой отмены — **пройдена**. Г0 закрыт живым замером (24.1%).
**Блокеров до реализации не осталось**; арка ждёт своей очереди в плане
(после пер-клиентного сторожа и Хайку), а не разрешения.

---

## 9. Открытые вопросы владельцу

1. ~~Окно суток на клиента: 24/7, 12ч или по расписанию лидов?~~ →
   **ЗАКРЫТ решением 19.08: умолчание 12ч/сут, значение — ИЗ КОНФИГА клиента.**
   Мотив владельца: безубыток вдвое ниже, а ночной трафик у обеих боевых
   клиенток близок к нулю — платить за него незачем. У клиента с ночными
   лидами окно другое, и это ЕГО настройка, а не наша константа.
2. ~~Порог включения: с какого объёма?~~ → **ЗАКРЫТ решением 19.08: порог не
   константа, а поле конфига клиента** (см. §10).
3. Принимаем ли, что тихим клиентам keep-alive не включается вовсе? — да,
   ровно это и делает порог из §10.

---

## 10. ТРЕБОВАНИЯ ВЛАДЕЛЬЦА 19.08 (обязательны к реализации)

Добавлены к спеке после решения строить арку. Мотив общий: клиентов будет
шесть и они РАЗНОГО объёма, поэтому ни одно число про объём не имеет права
быть константой в коде.

1. **Пер-клиентный тумблер.** Keep-alive включается для клиента, а не для
   инсталляции. Поле в `settings.yaml` клиента, как `strict_knowledge` и
   `honesty_mode`. Команд пульта не добавлять (тумблеры клиента — конфиг).
2. **Порог включения по объёму — ИЗ КОНФИГА, не константой.** Значение по
   умолчанию пересчитано 19.08 вместе с §4: безубыток при окне 12ч/сут — 13.2
   диал/мес, значит дефолт **25 диал/мес** (тот же запас ×1.9). Живёт как
   дефолт загрузчика, а не как литерал в горячем пути.
5. **Окно суток — поле клиента, умолчание 12ч/сут** (§9 п.1, решение 19.08).
6. **Приёмка Г4 — на volska, с контролем «одинаковое окно дней до и после»**
   (§4.1, решение 19.08).
3. **Правка `cache_health.consecutive_misses` — В ТОЙ ЖЕ АРКЕ, не после.**
   См. §5: прогрев рвёт цепочку промахов и ослепляет детектор. Арка,
   выключающая сторожа промахов, без этой правки не мержится.
4. Сторожа к пунктам 1–3 пишет **не тот автор**, что писал плановый код
   (общее правило: тест и код не должны наследовать одно допущение).
