# Спека: сторож покрытия бэкапа — «не назван нигде» как отдельное состояние

**Дата:** 24.08.2026 · **Заведено по:** DEV-70 · **Статус:** ✅ ОДОБРЕНА 24.08
**Кода нет.** Замер сделан, оба вопроса владельцу закрыты (§0).
**Живых клиентов не трогает:** всё описанное — чтение диска и два литеральных
списка.

---

## §0 Что решает эта спека и что решает владелец

**Спека решает:** какой инвариант обязан держаться, чтобы объект, живущий в
одном экземпляре, не мог появиться молча; в какой форме его проверять; и в
каком ПОРЯДКЕ это заводить, чтобы сторож не родился красным.

**Владелец решил оба вопроса 24.08:**

1. **563 файла разбирает сам** (§5 — предложение принято как основа разбора,
   не как решение). Полдня из §9 заложены верно;
2. **проба в лампу — ДА, семнадцатой.** Формулировка владельца, и она же
   объясняет, почему теста мало: «она не про инцидент, а про то, что мы
   перестали думать о новом файле; такое иначе не всплывает никогда».

**Порядок §4 принят целиком:** разбор → списки → сторож. Формулировка
владельца: «зелёный по построению в момент рождения — единственный способ,
чтобы он остался сигналом».

---

## §1 Замер 24.08 — с чего это началось

Сверка состава бэкапа под скелет политики (DEV-68) показала: клиентский набор
берёт РОВНО ДВА вида файлов на слаг (`app/services/state_backup.py:415-419`) —
боевую базу и `requisites.yaml`. Всё остальное содержимое клиента лежит в git.

Отсюда настоящее правило системы: **бэкапится то, чего не держит git.**
Правило верное. Оно нигде не записано, ничем не проверяется и существует как
совпадение двух литеральных списков, которые правит человек.

Прямой замер по диску (`state/`, файлы вне git и вне `CRITICAL_PATTERNS`):

* **1748 файлов, 2.4 ГБ** всего;
* из них **563 структурных (5.8 МБ)** — `.md`, `.json`, `.jsonl`, `.db`,
  `.yaml`, `.txt`; остальное медиа, `audit/*.etl` и профиль браузера.

Среди структурных — вещи, существующие в ОДНОМ экземпляре:

| файл | размер | свежесть |
|---|---|---|
| `state/dev_backlog_pipeline.md` | 208 КБ | сегодня |
| `state/panels_demo.db` | 192 КБ | 9 суток |
| `state/ask_owner.jsonl` | 155 КБ | сегодня |
| `state/decisions_archive.jsonl` | 113 КБ | 10 суток |
| `state/panel_events.jsonl` | 55 КБ | сегодня |
| `state/auth_canary.jsonl` | 20 КБ | вчера |
| `state/secrets_ledger.md` | 1.3 КБ | 8 суток |

Первый в списке — бэклог DEV: журнал решений, куда сегодня легли DEV-70 и
DEV-71. Он не в git (gitignored) и не в бэкапе.

## §2 🔴 Почему «просто добавить в список» — неверный ответ

Списков **два, и они разного знака**:

* `CRITICAL_PATTERNS` — что вывозить (allowlist, `state/`, 14 дней);
* `FORBIDDEN_PATTERNS` — что не вывозить НИКОГДА (сессии, `.env`,
  `entropy.bin`, `ig_accounts.json`, `google_oauth_token.json`).

Второй список — не забывчивость, а решение: session-файл это полный доступ к
аккаунту клиента, и вывезти его опаснее, чем потерять.

Значит у файла сегодня **три возможных состояния**, а понятий в коде **два**:

| состояние | что значит | есть ли в коде |
|---|---|---|
| назван в allowlist | вывозится | **да** |
| назван в forbidden | не вывозится осознанно | **да** |
| **не назван нигде** | **никто про него не думал** | **НЕТ** |

Третье состояние — единственное опасное, и именно его в системе не
существует как понятия. Объект в нём не попадает и в `missing`: это поле
заполняется только для путей, которые ИСКАЛИ. Бэкап проходит зелёным, честно
сложив всё, что ему назвали.

Отсюда форма сторожа: он ловит не «файл не в бэкапе» (это норма для доброй
половины диска), а **«про файл не принято решение»**.

## §3 Инвариант

> Файл под `state/`, которого нет в git, обязан быть назван РОВНО в одном из
> двух литеральных списков: вывозимых или запрещённых к вывозу. Файл, не
> названный ни в одном, — красное.

Три свойства, без которых инвариант не работает:

1. **fail-closed:** нераспознанное состояние — красное, а не пропуск. «Не
   знаем, что это» и «решили не вывозить» обязаны быть различимы, иначе
   третье состояние опять исчезнет;
2. **списки остаются ЛИТЕРАЛЬНЫМИ и правит их человек.** Список, выведенный
   обходом диска, согласен с диском по определению и молчит ровно там, где
   человек забыл ([[jarvis-literal-lists-not-introspection]]);
3. **исключения — тоже список, а не регулярка по «мусору».** Эфемерное
   (`*.pid`, `*.lock`, `*heartbeat*`) исключается поимённо: расширяющийся
   шаблон однажды накроет то, что накрывать не собирались.

## §4 🔴 Порядок заведения: сторож, рождённый красным, будет выключен

Если завести пробу сегодня, она загорится на 563 файлах и станет фоном. Сигнал,
всегда красный при нормальной работе, — не сторож
([[jarvis-gate-mutates-the-deploy-tree]], тот же класс).

Поэтому порядок строгий:

1. **проход человека по 563 файлам** (§5) — каждая группа получает решение:
   вывозим / не вывозим / эфемерное;
2. списки дополняются по итогам прохода, набор пересобирается;
3. **только теперь** заводится сторож — и он зелёный ПО ПОСТРОЕНИЮ в момент
   рождения;
4. дальше любая новая непокрытая вещь краснеет ОДНА, и её видно.

Шаг 3 без шагов 1–2 не имеет смысла, шаги 1–2 без шага 3 протухнут за месяц.

## §5 Классификация — предложение, решает человек

Замер по группам (файлов / МБ / свежесть самого свежего):

| группа | файлов | МБ | свежесть | предложение |
|---|---|---|---|---|
| `personas/**` (видео, кадры) | 453 | 2053 | сегодня | не вывозим: медиа-крыло заморожено, объём |
| `audit/*.etl` | 72 | 327 | сегодня | не вывозим: трассы, одноразовые |
| `browser_profile/**` | 283 | 26.7 | 50 сут | не вывозим |
| `logs/**` | 112 | 5.1 | сегодня | 🔴 **отдельный разговор:** см. DEV-69 (в логах лежит текст переписки) |
| `agent_mesh/**` | 184 | 2.3 | сегодня | решить: рабочее состояние или след экспериментов |
| `backups/**` | 73 | 1.35 | вчера | не вывозим: копии копий |
| **корень `state/`** | **60** | **0.88** | сегодня | 🔴 **здесь всё ценное — разобрать поимённо** |
| `dev_tasks/**` | 165 | 0.01 | 14 сут | частично в allowlist уже; добить хвост |
| `startup_logs`, `runpod`, `designs`, `landings`, `games`, `runtime_exec`, `obsidian_runtime`, `smart_prompts_cache` | ~150 | 0.3 | 100–130 сут | не вывозим: мёртвое |

Из шестидесяти файлов в корне `state/` в решение просятся семь из таблицы §1
плюс `chatter_clients.json` и `ops_watchdog_state.json` (состояние дедупа
алертов: без него после переезда сторож заново разошлёт всё, что уже слал).

Эфемерное из того же корня — `*.pid`, `*.lock`, `*heartbeat*`,
`*_offset.json` — в список исключений.

### §5.1 ✅ РАЗБОР ЗАКРЫТ 24.08 — решения владельца

Крупные группы приняты как предложены. Корень `state/` принят поимённо, без
добавлений. Три решения названы отдельно, потому что меняют не только список:

1. **`agent_mesh/**` — НЕ вывозим.** Формулировка владельца: «пишется, но это
   инерция, решений на нём не строим; понадобится — заметим». Важно для
   сторожа: это осознанное «не вывозим», а не «не подумали», и лежать оно
   обязано в списке запрещённых, а не в тишине — иначе третье состояние (§2)
   вернётся ровно там, где его только что закрыли;
2. **`state/conversations/*.jsonl` — ВЫВОЗИМ, и это ПДн.** Год хранения,
   удаление по требованию. Отсюда следствие за пределами этой спеки: файлы
   попадают в скелет политики отдельным источником (`docs/legal/data-inventory.md`
   §1.1), потому что человек в них тот же, а ключ ДРУГОЙ формы — голый peer id
   против `<external_id>:<персона>`. Замер: 4 файла, 89 КБ, `MAX_HISTORY = 100`
   (обрезка по СЧЁТУ, сто первая строка исчезает молча), `content` режется до
   1000 символов, а идентификатор человека стоит в ИМЕНИ файла;
3. **`logs/**` остаётся в DEV-69.** Вопрос там не «вывозить ли», а «писать ли
   туда текст переписки вообще» — сначала решается это, и только потом файл
   получает место в одном из двух списков.

🔴 **Следствие для порядка §4:** пункт 1 закрыт, списки можно дополнять.
Единственное, что держит сторожа от рождения, — решение по `logs/**` из
DEV-69: пока оно не принято, эти 112 файлов не попадают ни в один список, и
проба родится красной на них. Значит либо DEV-69 решается раньше, либо
`logs/**` получает ВРЕМЕННУЮ запись в исключениях с явной пометкой «до
DEV-69» — второе честнее записать в списке, чем держать в голове.

## §6 Форма: тест на сборке или проба в лампе

Спека НЕ выбирает за владельца, но называет цену обоих:

* **тест на сборке** — читает диск в момент прогона суиты. Дёшево, ловит на
  мерже, но молчит между мержами и зависит от того, что на машине разработчика
  лежит то же, что на боевой;
* **проба в `ops_watchdog`** — читает боевой диск каждые 30 с, красное уходит
  владельцу. Ловит появление файла в тот день, когда оно случилось. Цена —
  ещё одна проба в лампе (сейчас их 16) и риск шума, если §4 не соблюдён.

Рекомендация: **оба, но не одновременно.** Сначала тест (он же фиксирует
литеральные списки пином в обе стороны), проба — вторым шагом, после того как
проход §5 доказал, что зелёное достижимо.

✅ **РЕШЕНО 24.08: проба заводится, семнадцатой.** Довод владельца снимает
спор между двумя формами: проба здесь ловит не инцидент, а то, что про новый
файл перестали думать, — а такое не всплывает вообще никогда, пока кто-нибудь
не полезет за копией и не найдёт пустоту.

Следствия для реализации, названные здесь, чтобы не всплыли на приёмке:

* ключ пробы отдельный (не подвешивать к существующим), ярлык — человеческой
  фразой, как `chatter_roster`, а не сырым ключом;
* причина в алерте обязана НАЗЫВАТЬ файлы, а не их число: «3 новых файла вне
  списков» без имён отправляет владельца искать руками;
* дедуп по КЛАССУ, а не по значению: имя нового файла в причине сделает каждую
  запись уникальной и выключит дедуп по построению (ровно грабля DEV-66);
* проба обязана отличать «список не прочитан» от «непокрытых нет» — второе
  зелёное, первое `unreadable`, а не тишина.

## §7 Второй вопрос того же класса: срок

У наборов разные сроки — 14 дней у `state`, 365 у клиентского (DEV-46 §9.3,
где один порог на два класса тихо съедал годовой набор).

Объект, положенный «куда попало», молча получает чужой срок. Инвариант §3
отвечает на вопрос «вывозим ли», и не отвечает на «на сколько». Второй пин:
**у каждого вывозимого объекта срок назван явно**, а не унаследован от того,
в какой список он случайно попал.

## §8 Что эта спека не решает

* **что из 563 файлов ценно** — §5 это предложение, а не решение;
* **очередь входящих веб-канала** — она станет третьим видом объекта в
  клиентском наборе, и это работа DEV-70 п. 1, не эта спека;
* **логи с текстом переписки** — они попали в таблицу §5, но решать про них
  надо не «вывозить ли», а «писать ли вообще» (DEV-69).

## §9 Оценка

| работа | дни |
|---|---|
| проход человека по группам §5 (не наша работа, но без неё дальше нельзя) | 0.5 |
| тест-сторож + литеральные списки исключений | 1–2 |
| проба в лампе (если владелец скажет «да») | 1 |
| пин срока §7 | 0.5 |

**Итого 2.5–4 дня**, из них полдня — решение владельца, а не код.
