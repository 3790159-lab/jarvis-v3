# ARCHITECTURE: дашборд chatter — спека (без кода)

**Дата:** 2026-07-22 · **Ветка:** `arc/dashboard-spec` · **Опора:** AUDIT.md (фаза 1) + COMPETITORS.md (фаза 4) + PROBLEMS.md (фаза 3) · **Статус:** черновик спеки для решения Даниила. Кода нет.

**Дефолты этой спеки** (по указанию — спорное помечено 🟠 в §11):
- Дисклоуз: **honest by default** (тренд-2026 «прозрачность как доверие», COMPETITORS §3).
- Сегмент: **персональные продавцы** (риелторы, фрилансеры, эксперты) — визард и метрики под них.

---

## 1. Цель и принципы

**Цель.** Онбординг и ведение клиента **без ручной правки yaml и без SSH**. Мультиклиентность с первого дня. Дашборд — это **пульт чужих бизнесов**, поэтому auth и изоляция секретов — не фича, а условие существования (PROBLEMS P1/P2).

**Принципы:**
1. **Тонкий слой, не переписывание.** Дашборд управляет тем, что уже есть в chatter: 4 файла конфига на клиента, `.versions/`, `control_events`, heartbeat, гардиан, `/reload`, `yaml_edit`. Ядро (`run.py`, `telethon_run.py`, `core/*`) **не рефакторим** — дашборд читает его состояние и пишет его конфиги, а не лезет внутрь конвейера.
2. **Файлы конфига остаются источником правды.** Дашборд-редактор пишет ровно те же `persona.md/knowledge.md/playbook.md/settings.yaml`, что раннер уже умеет читать и hot-reload'ить. Каждая правка → снапшот в `.versions/` (уже есть) → откат из UI бесплатно.
3. **Control-plane / data-plane split.** Дашборд (control+view) отдельно от раннеров (data). Падение дашборда не роняет живые диалоги; падение раннера видно в дашборде через heartbeat.
4. **Безопасность с первого дня.** Session-файлы и `.env` дашборд **не отдаёт наружу никогда** (PROBLEMS P1). Auth обязателен до первого клиента.

---

## 2. Что переиспользуем vs что строим

| Уже есть в chatter (переиспользуем) | Строим в дашборде |
|---|---|
| 4 файла конфига на клиента + строгая валидация (`config/loader.py`) | Веб-редактор этих файлов с валидацией в браузере |
| `yaml_edit.py` (точечная правка funnel_gate/honesty с сохранением комментариев) | HTTP-ручки поверх него для тумблеров |
| `.versions/` (снапшоты, `/rollback`) | Кнопка «откатить» в UI |
| `control_events` (журнал эскалаций/тапов/`bot_question`) | Аналитика читает эту таблицу |
| heartbeat + гардиан + semidemo-флаги | Control-plane, обобщающий semidemo-флаг в реестр клиентов |
| `create_client.py` (скаффолд 4 файлов из шаблона) | Онбординг-визард вызывает его логику |
| `telethon_login.py` (интерактивный логин → `.session`) | Визард подключения аккаунта (QR/код) поверх него |
| control-bot (эскалации, тапы, команды) | Лента/inbox дублирует карточки в вебе + ответ владельца |
| notifications (Telegram-алерты) | Событийная шина расширяет это до вебхуков/Sheets |

**Вывод:** ~70% механики уже написано. Дашборд — это HTTP-фасад + фронт + control-plane над раннерами + событийная шина.

---

## 3. Стек (с обоснованием)

| Слой | Выбор | Почему |
|---|---|---|
| Backend | **FastAPI** | Уже основание Jarvis (`app/` на FastAPI), общий стиль, async, автогенерация OpenAPI = наш API-контракт бесплатно. |
| Хранилище (control-plane) | **SQLite** (реестр клиентов, users дашборда, аудит) отдельно от per-client chatter-БД | Не смешивать пульт и переписку лидов; chatter-БД остаётся per-client как сейчас. |
| Фронт | **Лёгкий SPA** — HTMX+Alpine или минимальный React | Дашборд — форма-центричный CRUD + лента + графики. Тяжёлый SPA избыточен. 🟠 выбор в §11. |
| Auth | **Session-cookie + пароль (argon2)**, MVP — один-два владельца-оператора; роли позже | Пульт чужих бизнесов → auth с первого дня (PROBLEMS P1). OAuth/SSO — v2. |
| Real-time | **SSE** (Server-Sent Events) для ленты/статуса | Односторонний поток событий, проще WebSocket, ложится на FastAPI. |
| Деплой | **Текущая инфра** — backend-гардиан-таск рядом с `:8010`, за тем же ENFORCE-middleware | Не плодить инфру; auth-middleware уже боевой (память: security-audit). |

---

## 4. Как дашборд управляет раннерами (ключевой технический вопрос)

Сегодня: раннер — **один процесс на Telegram-аккаунт**, поднимается гардианом, состав персон — из `active.yaml`, semidemo-флаг пинит окружение под volska.

**Мультиклиентность с первого дня** означает: **разные клиенты = разные Telegram-аккаунты = разные процессы раннера** (у каждого свой `.session`, своя БД, свой каталог конфига). Один процесс на несколько персон (как demo+demo2) остаётся частным случаем «один аккаунт, несколько персон», но новый бизнес — это новый аккаунт → новый процесс.

**Архитектура control-plane:**
- **Реестр клиентов** (SQLite дашборда): `client{ id, slug, tg_account_ref, session_path, db_path, config_dir, enabled, runner_state }`.
- **Обобщённый гардиан-механизм.** Semidemo-флаг (`state\chatter_semidemo_volska.flag`) — это уже прото-реестр на один клиент. Обобщаем: гардиан читает реестр клиентов (или per-client флаг-файлы, генерируемые дашбордом), для каждого `enabled` клиента держит живой раннер с его пинами окружения (`CHATTER_PERSONAS`, `TELETHON_SESSION`, `CHATTER_DB`).
- **start/stop/revert** из дашборда = запись в реестр (`enabled`/`runner_state`) + сигнал гардиану; гардиан приводит реальность к желаемому состоянию (поднять/убить процесс). Дашборд **не запускает процессы напрямую** — только декларирует желаемое состояние, гардиан исполняет (как сейчас с флаг-файлом). Это безопасно: тот же паттерн, что уже работает.
- **Живой статус** — дашборд читает heartbeat каждого клиента + PID-состояние от гардиана → зелёный/жёлтый/красный в модуле «Клиенты».
- **reload без рестарта** — дашборд записал конфиг → триггерит `/reload` этого клиента (существующий механизм) либо полагается на `auto_reload` по mtime. Рестарт нужен только при смене аккаунта/сессии.

> ⚠️ **Catch-up-риск (AUDIT §1.7) в UI.** Дашборд при включении клиента / открытии funnel_gate ОБЯЗАН показывать предупреждение: «на старте бот ответит на непрочитанное за 24ч через текущее состояние гейта». Гейт-тумблер — с подтверждением (как `/funnel_gate on confirm`).

**MVP-упрощение:** в v1 control-plane управляет клиентами, которые **уже подключены** (аккаунт залогинен оператором). Полная авто-провизия процесса из визарда (залогинил аккаунт в браузере → гардиан сам поднял раннер) — это стык визарда и гардиана, довести в v1 если успеваем, иначе v2 (§10).

---

## 5. Модули

### М1. Клиенты (список + управление)
- Карточки клиентов: имя, персона, аккаунт, **живой статус** (раннер/гардиан/heartbeat — зелёный/жёлтый/красный), последняя активность.
- Действия: **start / stop / revert** (через control-plane §4), открыть конфиг (М3), открыть ленту (М5), открыть аналитику (М6).
- Revert = откат конфига на предыдущий снимок `.versions/` (существующий `/rollback`).

### М2. Онбординг-визард
По модели Kommo «4 блока» (COMPETITORS §2), ложится на 4 файла конфига:
1. **Подключение Telegram-аккаунта** — QR или код (поверх `telethon_login.py`). ⚠️ Логин интерактивный (номер/код/2FA) — визард проводит оператора, `.session` пишется на сервере и **наружу не отдаётся** (P1). 🟠 QR vs код — §11.
2. **Анкета персоны** → `persona.md` (первая строка — проза для честного раскрытия, это уже требование `create_client.py`).
3. **Загрузка знаний** — файлы/текст/URL → `knowledge.md`. RAG-синк без переобучения (Respond.io-паттерн) — v2; v1 = собрать в один markdown.
4. **Выбор playbook-шаблона** → `playbook.md` (воронка, keyword-эскалации, цели). Плюс `settings.yaml` из шаблона `create_client.py` (brand-safety, тумблеры, allowlist).
- В конце — снапшот в `.versions/` + регистрация клиента в реестре (disabled, пока оператор не нажал start).

### М3. Редактор конфига
- persona / knowledge / playbook / settings в браузере, с **валидацией** (переиспользуем `loader.py` серверно: сохранение прогоняется через тот же парсер, опечатка в поле → та же ошибка, что ловит раннер).
- Улучшения из конкурентов: **few-shot примеры ответов** (Chatfuel) в persona-редакторе; **структурированные правила** вместо сырого промпта (Kommo, лимит N правил) — маппинг на `_STYLE_*`/playbook. Связано с PROBLEMS P12 (вынести хардкод-тексты в конфиг — предпосылка для полноценного редактора).
- Сохранение → снапшот `.versions/` → `/reload` без рестарта. Кнопка «откатить» (список версий).

### М4. Тумблеры
Поверх существующих `yaml_edit.py` + команд:
- **honesty** (honest / free_owner_liability) — с consent-флоу и именем клиента (как `/honesty ... confirm`; дефолт honest).
- **funnel_gate** (on/off) — с подтверждением + предупреждением о catch-up (§4).
- **pause / resume** — глобально и per-контакт (kill_switch + mute).
- **allowlist / denylist** — редактор списков.
Каждый тумблер — HTTP-ручка, пишет в тот же yaml + reload; поведение 1:1 с командами пульта (уже под тестами, фаза 2 §3).

### М5. Лента / inbox
- Карточки эскалаций (дубль того, что уходит в control-bot), takeover-события, деградации, `bot_question`-всплески — потоком через SSE.
- **Ответ владельца из дашборда** → отправляется тем же раннером в диалог лида (для лида = «сообщение персоны»), эквивалент ручного ответа с телефона. Ставит takeover-паузу (существующий механизм).
- **Кнопки как в карточке**: ▶️ вернуть · ⏸ ещё 1ч · 🔴 стоп · ✅ оставить боту — те же callbacks, что в control-bot.
- Handoff-нарратив (Respond.io §2): «AI глохнет мгновенно при перехвате» — у нас уже есть (re-check mute перед каждой отправкой), подать явно в UI.

### М6. Аналитика
Читает `control_events` + chatter-БД (только агрегаты, не сырую переписку без нужды — P8):
- Диалоги, лиды, эскалации, время ответа, воронка (стадии `contacts.state`).
- **Счётчик «ты бот?»** (`bot_question`, реализован в фазе 2) — пилотный сигнал доверия/подозрения.
- **Расход токенов/$** — прогноз из AUDIT §3; после prompt-caching (P3) — факт. 🟠 источник факта расхода — §11.
- Операционные метрики уровня Wati/Umnico (§2): время ответа, активность по клиентам.

---

## 6. Событийная шина + адаптеры (интеграционный зазор)

**Контракт событий** (единый для всех адаптеров), см. `API_CONTRACT.md`:
- `lead.created` · `lead.qualified` · `lead.escalated` · `lead.status_changed` · `dialog.taken_over` · `dialog.resumed` · `bot_question.asked`.
- Каждое событие: `{ event, client_id, contact_ref, ts, payload }`. Источник — тот же слой, что пишет `control_events` (расширяем эмиттер, не трогая конвейер).

**Адаптеры как плагины** (интерфейс `emit(event) -> None`):
- **MVP:** generic **webhook** (POST на URL клиента, HMAC-подпись — уже есть паттерн `N8N_JARVIS_WEBHOOK_SECRET`) + **Google Sheets** (append строки; креды через `GOOGLE_SERVICE_ACCOUNT_JSON`, уже в `.env`).
- **Интерфейсом (v2):** amoCRM/Kommo, CSV-экспорт. В спеке — контракт и интерфейс адаптера; реализация двух в MVP, остальные — заглушки под тем же интерфейсом.

> Это закрывает разрыв с CRM, не встраивая CRM внутрь: chatter остаётся «AI-продавец от первого лица», а сделки/воронка живут в CRM клиента через шину (в отличие от Kommo, где CRM встроена — наш выбор осознанный, COMPETITORS §3).

---

## 7. Auth и безопасность (условие, не фича)

- **Auth с первого дня**: session-cookie + argon2-пароль, за тем же ENFORCE-middleware, что `:8010` (память: security-audit-highs). MVP — оператор(ы) владельца; мультиарендная ролевая модель (клиент видит только свой кабинет) — v2. 🟠 §11.
- **Session-файлы/`.env` наружу не отдаются никогда** — ни через API, ни в логах, ни в ленте (P1/P2). Дашборд оперирует ссылками (`session_path`), не содержимым.
- **ПДн лидов** (P8): аналитика по умолчанию — агрегаты; полная переписка — только по явному действию оператора, с аудит-логом доступа.
- **Аудит действий дашборда** — отдельная таблица (кто/что/когда переключил): критично для пульта чужих бизнесов.
- Прежде чем пускать реальных клиентов — закрыть блокеры P1/P2 (шифрование секретов at rest). Дашборд их **обостряет** (централизует доступ к N чужим аккаунтам), поэтому это зависимость, а не параллельная задача.

---

## 8. Модель данных (control-plane, отдельная БД)

- `client{ id, slug, display_name, tg_account_ref, session_path, db_path, config_dir, enabled, runner_state, created_at }`
- `dashboard_user{ id, login, argon2_hash, role, created_at }`
- `audit_log{ id, user_id, action, target, detail, ts }`
- `integration{ id, client_id, type(webhook|sheets|...), config_json, enabled }`
- Аналитика и лента **не дублируют** данные — читают per-client chatter-БД (`control_events`, `contacts`, `messages`-агрегаты) по `db_path` из реестра.

---

## 9. Деплой

- FastAPI-приложение рядом с текущим backend, свой guardian-таск (S4U/Highest, как `JarvisBackendGuardian`), за ENFORCE-middleware.
- Control-plane и раннеры — разные процессы; гардиан(ы) раннеров расширяются на реестр клиентов (§4).
- Откат дашборда не влияет на живые диалоги (раннеры автономны).

---

## 10. MVP-срез

**v1 (2–3 недели) — «вести уже подключённого клиента без yaml и SSH»:**
- Auth (§7, базовый) + аудит-лог.
- М1 Клиенты: список, живой статус, start/stop/revert над клиентами в реестре.
- М3 Редактор конфига (4 файла, валидация, `.versions/`-откат, reload).
- М4 Тумблеры (honesty/funnel_gate/pause/allowlist) — поверх `yaml_edit`.
- М5 Лента: карточки эскалаций + ответ владельца + кнопки (SSE).
- М6 Аналитика: базовая (диалоги, эскалации, `bot_question`, прогноз $).
- Событийная шина: контракт + **webhook + Sheets** адаптеры.

**v2 — «онбординг с нуля в браузере + мультиарендность»:**
- М2 Онбординг-визард целиком (QR-логин аккаунта → авто-провизия раннера гардианом).
- Мультиарендная ролевая auth (клиент видит только свой кабинет), OAuth/SSO.
- Адаптеры amoCRM/Kommo + CSV; RAG-синк знаний без переобучения.
- Редактор персоны с few-shot примерами; факт расхода токенов после prompt-caching (P3).

**Зависимость-блокер:** P1/P2 (шифрование секретов) закрыть **до** пуска реальных чужих клиентов через дашборд — он централизует доступ к N аккаунтам.

---

## 11. Открытые вопросы (для решения Даниила)

| # | Вопрос | Дефолт спеки | Почему спорно |
|---|---|---|---|
| 🟠 O1 | Дисклоуз по умолчанию | honest by default | Тренд-2026 за прозрачность, но «неотличимость» — часть ценностного предложения. Решает позиционирование. |
| 🟠 O2 | Сегмент | персональные продавцы (риелторы/фрилансеры) | Меняет шаблоны playbook и метрики визарда. Шире (SMB) = другой визард. |
| 🟠 O3 | Фронт-стек | HTMX+Alpine (лёгкий) vs React | Скорость MVP vs будущая интерактивность ленты/аналитики. |
| 🟠 O4 | Подключение аккаунта в визарде | QR (как BotHelp) | QR удобнее, но Telethon-логин интерактивный (код/2FA) — QR может не покрыть все кейсы; fallback на код. |
| 🟠 O5 | Auth-модель MVP | один оператор владельца | Мультиарендность (клиент сам заходит) сдвигает сроки и требует изоляции раньше. |
| 🟠 O6 | Источник факта расхода $ | оценка до P3, факт после prompt-caching | Точный per-client биллинг требует учёта usage от Anthropic — отдельная работа. |
| 🟠 O7 | Приоритет P1/P2 vs скорость MVP | закрыть до реальных клиентов | Можно ли катать MVP на своих/демо-аккаунтах, пока секреты не зашифрованы? |

Черновик API-контракта: `docs/dashboard/API_CONTRACT.md`.
