# Журнал: цепочка проверки бэкапа — шов дрила и counts в манифесте

Ветка `fix/backup-drill-verifiable` от `a83ffb66`, дерево
`C:\jarvis_worktrees\drill-chain`. Порядок задан владельцем 04.09:
1) шов дрила · 2) манифест с counts · 3) живой прогон дрила · 4) DEV-90 B.

Не мержу, не деплою, раннер не трогаю.

---

## Задача 1 — шов `.env` у дрила: ЗАКРЫТА

- [x] **Сторожа ДО правки** — `tests/test_restore_drill_env_seam.py`.
      Запускают ОТДЕЛЬНЫЙ процесс с вычищенными R2-переменными, ровно как это
      делает планировщик. До правки: `REFUSED:R2ConfigError` — то есть сторож
      исполняет именно ту ветку, ради которой написан.

      **Встречный обязателен и есть:** без импорта дрила конфигурация собраться
      НЕ должна. Без него зелёное могло прийти от протёкшей переменной родителя
      или от `sitecustomize.py`.

      🔴 **Третий путь загрузки `.env`, найденный при этом:** в корне лежит
      `sitecustomize.py`, который сам зовёт `load_dotenv`. Интерпретатор
      подхватывает его, только если корень попал в `sys.path` на старте
      (запуск ИЗ корня); задача запускает скрипт ПО ПУТИ, и тогда `sys.path[0]`
      это `scripts/`. Отсюда худший вид разницы: из консоли работает, из
      планировщика падает. Поэтому cwd подпроцесса в стороже уведён во
      временный каталог, а на `sitecustomize` не полагаемся.

- [x] **Правка:** модульный `import app.env_bootstrap` сразу после
      `sys.path.insert`, ДО первого чтения окружения. Сторожа: **2 passed**.

- [x] **ПРИЁМКА — живой запуск ТОЙ ЖЕ формой, что у задачи** (скрипт по пути,
      cwd = корень дерева), лог `drill-after-task1.log`:

      | | до правки | после |
      |---|---|---|
      | где спотыкается | `R2ConfigError`: конфигурация бакета не собралась | **дошёл до бакета и до манифеста** |
      | сообщение | missing R2_ACCOUNT_ID, … | «ни у одной записи манифеста нет counts» |

      Причина №1 закрыта: дрил доходит до данных. `rc` по-прежнему 2, но уже
      по причине №2 — это задача 2.

---

## Задача 2 — манифест без counts: КОРЕНЬ ОКАЗАЛСЯ ДРУГИМ

**Писатель НЕ сломан.** `run_client_backup` кладёт `counts` каждой записи вида
`db` — код на месте. Манифест ЗАТИРАЮТ.

### Замер по датам (только чтение)

| дата | объекты | манифест |
|---|---|---|
| 01.09 | db 01:01, db 01:01, requisites **11:01** | **11:01**, 1 запись, counts НЕТ |
| 02.09 | всё 01:01 | 01:01, **3 записи, у обеих db counts ЕСТЬ** |
| 03.09 | db 01:01, db 01:01, requisites **23:10** | **23:10**, 1 запись, counts НЕТ |
| 04.09 | db 01:01, db 01:01, requisites **16:28** | **16:28**, 1 запись, counts НЕТ |

Ночная задача (01:01 UTC = 04:00 локально) пишет ПРАВИЛЬНЫЙ манифест: три
записи, у баз есть `counts` — видно по 02.09. А позже в тот же день приходит
ВТОРОЙ, ЧАСТИЧНЫЙ прогон: он заливает только `requisites.yaml` и
**перезаписывает манифест описанием из одной записи**. Базы при этом остаются
в бакете нетронутыми — но перестают быть описанными.

### Кто делает частичный прогон — ЭТО СУИТА

`tests/test_state_backup_script.py::test_main_success_sends_summary_and_returns_zero`
зовёт НАСТОЯЩИЙ `main()` скрипта, подменяя `run_backup`, `rotate_all_backups`
и `send_telegram`, — но **`run_client_backup` не подменяет**. А `main()` его
зовёт (`scripts/state_backup.py:117`). В worktree `.env` — ССЫЛКА на живой,
значит боевые ключи R2 на месте; `.secrets/*.db` в worktree нет, а
`requisites.yaml` туда КОПИРУЕТ `new_worktree.py`. Отсюда ровно один залитый
объект и затёртый манифест.

Даты совпадают с прогонами суиты, а 02.09 уцелело потому, что в тот день
полного прогона после ночной задачи не было.

### 🔴 Я ВОСПРОИЗВЁЛ ЭТО ЖИВЬЁМ, ХОТЯ ХОТЕЛ ОБРАТНОГО

Доказательство строилось как БЕЗОПАСНОЕ: подменить `r2_storage.upload_file`
регистратором и посмотреть, какие ключи «уехали бы». Регистратор не сработал —
**значение по умолчанию `upload_file` в сигнатуре `run_client_backup`
привязано на этапе ОПРЕДЕЛЕНИЯ функции**, поэтому подмена атрибута модуля
после импорта на него не влияет. Скрипт сделал НАСТОЯЩУЮ заливку: в бакете
появились `requisites.yaml` и `manifest.json` от **17:25:54 UTC**.

Ущерб равен ежедневному ущербу от суиты и ничего не удалил: обе базы за 04.09
на месте (01:01), затёрт только манифест — он и так был затёрт с 16:28.
Но обычная защита «подменить функцию в модуле» здесь НЕ РАБОТАЕТ, и это надо
знать всем, кто будет писать сторожа вокруг заливки.

### Что из этого следует делать (жду решения владельца)

1. **неполный набор обязан КРАСНЕТЬ** — манифест несёт ЛИТЕРАЛЬНЫЙ список
   ожидаемых объектов и признак полноты; частичный прогон не может выглядеть
   как описание целого набора, а дрил на неполном наборе даёт `rc=1`
   (вердикт записан, `ok=false`), а не `rc=2` «проверить нечем»;
2. **суита не смеет лить в боевой бакет** — застава fail-closed в
   `run_client_backup`. Это правка боевого кода заливки, поэтому спрашиваю.

## Задача 3 — живой прогон дрила: ЧАСТИЧНО СДЕЛАН И ЗЕЛЁНЫЙ

- [x] **ДРИЛ ПРОШЁЛ НА НАБОРЕ 02.09, `rc=0`.** Манифест того дня уцелел
      (три записи, `counts` у обеих баз), поэтому дрил удалось прогнать
      ЦЕЛИКОМ — не дожидаясь правок:

          ✅ дата 2026-09-02, объектов 3, баз сверено 2: снимок открыт
             настоящим слоем хранения (chatter.storage.db.Store),
             величины сошлись

      Вердикт `verdict-2026-09-02.json`: `ok=true`,
      expected == actual == `{dialogs: 13, messages: 445,
      last_message_at: 1788246564.89}`. То есть пройдены ВСЕ шаги: скачано,
      sha256 шифротекста сверен с манифестом, расшифровано приватным ключом,
      база открыта настоящим `Store`, величины сошлись.

      🔴 Вердикт направлен в дерево ВЕТКИ намеренно: он относится к набору
      **двухдневной давности**, и класть его на место живой улики значило бы
      погасить лампу зелёным за позавчера.

- [ ] **Набор ЗА СЕГОДНЯ дрилом не проверен** и не может быть, пока манифест
      затирается: объекты в бакете есть и того же размера, что 02.09, но они
      НЕ ОПИСАНЫ — сверять sha256 не с чем.

## Пункт 4 — мутационный гейт на решения заставы

- [ ] **ПРОГОН.** `python scripts/mutate_client_backup_guard.py`,
      лог `docs/drill-chain/gate-guard.log`. Пять мишеней: 1 признак не
      спрашивается · 2 застава отключена · 3 сравнение с АТРИБУТОМ модуля ·
      4 загрузчик не спрашивается · 5 `main()` не пробрасывает отказ.

      **ПРЕДСКАЗАНИЕ: 5 из 5, слепых 0.** Основание не «должно работать»:
      каждой мишени отвечает ОТДЕЛЬНЫЙ сторож, и сторож №6 (сравнение с
      объектом) дописан специально под мишень 3, иначе она была бы слепой.

      **Чем это может оказаться враньём:** мишень 5 ссылается на
      несуществующее имя `_NeverRaised`. Если она даст не `failed`, а ошибку
      СБОРА (rc 2), критерий гейта назовёт её слепой — и это честно, но
      причина будет не та, что я думаю. Разберу по тексту.

      **ЕСЛИ СТРОКА ОСТАЛАСЬ НЕОТМЕЧЕННОЙ — в дереве может лежать МУТАНТ:**
      признак — расхождение `git hash-object` с `HEAD` по
      `app/services/state_backup.py` или `scripts/state_backup.py`.
      Лечение: `git checkout --` по этим двум путям.

## Пункт 5 — манифест: ожидаемый состав и признак полноты

- [ ] Неполный набор обязан краснеть; дрил на нём даёт `rc=1` с записанным
      вердиктом, а не `rc=2` «проверить нечем».

## Пункт 6 — полная суита

- [ ] Только ПОСЛЕ заставы: до неё каждый полный прогон портил боевой манифест.

## Пункт 7 — запись в `CLAUDE.md` рядом с DEV-26

- [ ] Привычный приём «подменить функцию в модуле» вокруг этого кода не
      работает: умолчания швов привязаны на этапе определения функции.

## Задача 4 — DEV-90 вариантом B

- [ ] Если останется время.
