# Ночная сессия 13.08.2026 — журнал

**Worktree:** `C:\jarvis_worktrees\night-session`, ветка `arc/night-2026-08-13`,
от транка `c1d8bf42`.
**Правила сессии:** TDD, мутационный гейт DEV-26, работа только в worktree,
**ничего не мержить и не деплоить без отмашки владельца.**

Если сессия оборвалась — читать этот файл сверху вниз, продолжать с первого
пункта без отметки «закрыто».

## План

| № | Задача | Статус |
|---|--------|--------|
| 1 | Панель Джарвиса: разбор 401 заново, факты с его браузера | **закрыто** — причина найдена |
| 2 | Навигация: ссылка «Ферма» ↔ обратно; 401 на `/panel/*` → форма входа | **закрыто** `ad399527` |
| 3 | Дизайн: 2–3 варианта палитры и типографики | **сделано ДО этой сессии** |
| 4а | `mutate_ops_watchdog.py`: дыра с байткодом | **закрыто** `c68238b4` |
| 4б | Разбор прироста красных 40 → 72+15 | не начато |

### Пункт 3 — уже закрыт до начала сессии
Три варианта собраны и отданы владельцу страницей выбора:
`https://claude.ai/code/artifact/10b49d7b-fa6e-473a-9d46-2669c7d9db8f`
(A «Кабінет» светлая/IBM Plex Sans, B «Сапфір» тёмно-синяя/Inter,
C «Графіт» тёплая тёмная/Manrope). Рендеры на разметке демо-стенда, 390 px.
Живую панель не трогал. **Ждёт выбора буквы утром — в этой сессии не трогаю.**

---

## 1. Панель Джарвиса — разбор 401 заново

**Новый факт от владельца:** ручной набор адреса в адресной строке ТОЖЕ даёт
отказ. Это ломает прежнее объяснение: `SameSite=strict` режет переход по ссылке
из чужого приложения, но набор в адресной строке браузеры считают same-site и
cookie отправляют. Значит причина другая.

**Что уже установлено фактами (предыдущая сессия):**
- Запросы телефона ДОЕЗЖАЮТ: в логе доступа `GET /panel/jarvis 401` дважды,
  с адреса `100.116.181.70`.
- В той же сессии того же телефона `/panel/tamapi` → 200, и даже
  `POST /panel/tamapi/action` → 200. То есть cookie в той сессии работала.
- Блокировки event loop нет: страница 4.25 с, `/health` отвечает, гардиан
  за 17 минут не убил бэкенд ни разу.
- Cookie `panels_key` path=`/panel` покрывает `/panel/jarvis` по RFC 6265 —
  проверено и расчётом политики, и живым заходом с cookie-jar (200).
- Deny middleware по `/panel/jarvis` в `auth_canary.jsonl` НЕ писался.

**Отсюда следует:** на тех двух запросах cookie не была отправлена или не
подошла. Гипотезы, которые предстоит развести фактами:
- (H1) другой ХОСТ в адресе (MagicDNS-имя вместо IP) — cookie привязана к хосту;
- (H2) другой браузер/профиль/приватная вкладка;
- (H3) что-то режет заголовок на стороне мобильного браузера или Tailscale.

### ✅ ПРИЧИНА НАЙДЕНА. Это не SameSite и не панель Джарвиса.

**Факт 1. Отказ не привязан к панели Джарвиса.** В логе доступа того же
телефона, строка 442: `GET /panel/tamapi` → **401**. То есть к концу вечера
отказывали ОБЕ панели, а не одна. Прежняя рамка «джарвис не открывается, тамапи
работает» была верна только для первых минут.

**Факт 2. Лог не переживает рестарт** — в файле ровно один «startup complete»
(строка 54). Значит вся активность телефона произошла ПОСЛЕ 02:07:59, в одном
процессе, и порядок строк — настоящая хронология:

| строки | что | итог |
|--------|-----|------|
| 115–124 | вход: `/panel` → форма → `POST /panel/login` | 303, cookie поставлена |
| 124–186 | `/panel/tamapi`, `dynamics`, `POST .../action` | **200** — cookie работает |
| 231, 303 | `/panel/jarvis` | 401 |
| **442** | **`/panel/tamapi`** | **401** — сессия отвалилась целиком |
| 445–465 | `/panel/jarvis` ×4 | 401 |

**Факт 3 (решающий). С этого телефона ходят ТРИ РАЗНЫХ БРАУЗЕРА.**
`state/auth_canary.jsonl` пишет User-Agent на каждый deny, а иконки
(`/favicon.ico`, `/apple-touch-icon.png`) не публичны и денаятся всегда — они и
принесли UA с адреса `100.116.181.70`:

1. `Chrome/151.0.0.0 Mobile Safari` — Chrome мобильный
2. `SamsungBrowser/30.0 Chrome/143.0.0.0 Mobile Safari` — **Samsung Internet**
3. `Chrome/151.0.0.0 Safari/537.36` (без `Mobile`) — тот же Chrome в режиме
   «версия для ПК»

**Механизм:** cookie `panels_key` — host-only, HttpOnly, `Path=/panel`, и живёт
в банке ТОГО браузера, где был вход. Вошёл в одном, открываешь в другом —
cookie нет, `require_owner` отдаёт 401. Набор адреса руками это не лечит: дело
не в способе перехода, а в том, ЧЕМ открыто. Запросы `apple-touch-icon.png` от
Samsung Internet говорят, что панель ещё и добавлялась на домашний экран —
ярлык открывается своим браузером, и это четвёртая банка.

**Почему прежнее объяснение (SameSite) неполное:** оно верно как механизм, но
описывает только переход по ссылке из чужого приложения. Здесь ломается и
прямой набор — потому что причина не в переходе, а в банке cookie.

**Лечение = пункт 2 задания.** 401 на `/panel/*` обязан уводить на форму входа,
а не отдавать голый JSON. Тогда любой браузер лечит себя сам: открыл → форма →
ключ → внутри. Отдельного «фикса панели Джарвиса» не требуется — её нечего
чинить.

**Побочная находка:** на каждую загрузку страницы прилетает 3–4 запроса иконок
(`favicon.ico`, `favicon.png`, `apple-touch-icon*.png`), все 401. У панели нет
ни одной иконки — значит и ярлык на домашнем экране получает пустой квадрат.
В бэклог, не в эту сессию.

_Пункт 1 закрыт: причина установлена фактами, лечение — пункт 2._

---

## 2. Навигация и выход из отказа — ЗАКРЫТО

Коммиты `ad399527` (код + тесты) и гейт следом. **Не мержено, не задеплоено.**

- Браузеру (GET + `Accept: text/html`) отказ отвечает `303` на
  `/panel/login?next=…`; машине — прежний `401` JSON слово в слово.
- POST не редиректится никогда: иначе «оплачено» уводило бы на пустую форму,
  и человек решил бы, что записалось.
- `next` уходит в тот же белый список, что и форма входа.
- Обработчик повешен на СВОЙ класс-наследник `HTTPException` — общий
  обработчик приложения не тронут.
- Ссылки: «Ферма →» с клиентской панели, «← Клієнти» с фермы.
- Сторож на проводку в `app/main.py` отдельный: тесты поднимают своё
  приложение и о `main.py` не знают.

**Цифры:** 162 теста зелёные (`test_panels_web`, `test_panel_routes_owner_guarded`,
`test_panel_event_loop`, `test_panels_hierarchy`, `test_panels_denominators`,
`test_panels_stopall_ux`, `test_auth_enforce`).
Гейт DEV-26 `scripts/mutate_panels_no_dead_end.py` — **8 из 8 мутаций пойманы**.

⚠️ В worktree понадобился `git update-index --skip-worktree` на три файла с
CRLF-round-trip (`jarvis_claude_review_pack/app__main.py` и два
`artifacts/patch_backups/*.py`) — иначе `assert_clean` гейта отказывается
стартовать. Это известная грабля, не следствие правок.

---

## 4а. Дыра DEV-26 в `mutate_ops_watchdog.py` — ЗАКРЫТО

Коммит `c68238b4`. **Не мержено.**

Дыра подтверждена по исходнику: мутант писался голым `write_text`, подписи
`os.utime` не было вовсе. Из десяти гейтов дырявым был ровно один — остальные
девять подпись ставят. Здесь окно шире всех: **все 17 мутаций бьют в один файл**
`scripts/ops_watchdog.py`, то есть совпасть по размеру им проще всего.

**Честный итог перепрогона: 17 из 17 пойманы.** То есть дыра была открыта, но
ложного зелёного не дала — ни один сторож не оказался слепым. Прежние цифры
задним числом подтвердились; разница в том, что теперь они опираются на
проверку, а не на везение.

**Сторож поставлен на ВСЕ гейты, а не на этот скрипт** —
`tests/test_mutation_gate_bytecode.py`, 21 тест:
- каждая `scripts/mutate_*.py` обязана иметь `write_mutant`;
- две записи ОДНОГО РАЗМЕРА подряд обязаны получить разные mtime (проверяется
  поведение, а не наличие строки `os.utime`: подпись можно оставить и передать
  ей одно и то же значение);
- запись мимо помощника запрещена — прошлый раз дыра выглядела именно так,
  помощник был, им не пользовались.

Красным до правки был ровно `mutate_ops_watchdog`, девять остальных зелены —
сторож различает, а не красит всё подряд.

---

## 4б. Прирост красных 40 → 72+15 — РАЗБОР (в работе)

Метод: два полных прогона `pytest tests/` в ОДИНАКОВЫХ условиях (оба worktree,
в обоих одинаково отсутствует gitignored `chatter/clients/volska/requisites.yaml`)
и сравнение по ИМЕНАМ тестов, а не по числам — «стало на 32 больше» ничего не
говорит о том, те же это падения или другие.

- A = `arc/night-2026-08-13` (сейчас), worktree `night-session`
- B = `a9743d51` (транк на 10.08 20:03), worktree `baseline-1008`, detached

### Что приехало между 10.08 и сейчас (по git, до цифр)

**31 новый тестовый файл**, из них **20 — платежи** (`test_payments_*`): между
этими датами целиком приземлилась арка `chatter/payments/` (18 новых модулей).
Остальные новые — панели (5), мутационные харнессы (3), bind, config_commit,
verify_bundle.

**Ключевая деталь:** 11 тестовых файлов ссылаются на
`chatter/clients/volska/requisites.yaml`, который **gitignored и существует
только в `C:\jarvis`**. В любом worktree их падение — СРЕДА, а не код. Из этих
11 два файла (`test_payments_tiers_dialogue`, `test_payments_tier_texts`)
созданы 12.08 — то есть часть «прироста» могла приехать не с поломкой, а с
местом замера.

_Цифры — ниже, после прогонов._
