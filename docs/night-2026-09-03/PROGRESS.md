# Ночная автономная сессия 03.09.2026 — прогресс

Ветка `arc/night-2026-09-03`, дерево `C:\jarvis_worktrees\night-0309`,
отрезана от транка `a83ffb66`. Транк не трогаю (DEV-25), живое не трогаю.

## Выбор задач и почему именно эти

Критерий владельца: «деньги или риск потери», не громкость.

### A. sales-competence C-1 — ПРЯМЫЕ ДЕНЬГИ ⬅ главная

`docs/superpowers/specs/2026-08-04-chatter-sales-competence.md` §1.2 и §4.

Спека доказала форензикой: бот не закрывает сделку **не из-за модели, а из-за
нашего кода**. `_findings` в `chatter/core/guardrails.py` держит правило
`large_number`: любое число ≥ 100, которого нет в `knowledge.md`, объявляется
необеспеченным. Функция видит только `reply` и `knowledge` — истории диалога
она не видит вообще. Значит **бюджет, который назвал сам лид**, режется как
выдумка бота и заменяется на «узгоджуємо індивідуально», а если редакция не
вычистила всё — ответ подавляется целиком.

Почему это деньги, а не удобство: пока правило не изменено, **ни один playbook
не заставит бота считать по вводным клиента**. Арифметика на числах лида (CPL,
стоимость гостя, охват под бюджет) почти всегда даёт числа ≥ 100, которых нет
и не может быть в прайс-листе. Это прямо блокирует критерий приёмки «использует
цифры клиента».

Беру этап **C-1** целиком — он по спеке ни от чего не зависит и не требует
контента от владельца (C-0):

* **D2-1** число из сообщений лида в этом диалоге — обеспечено, с границей:
  пересказ чужого числа можно, НАША цена этим числом — нельзя;
* **D2-3** одна и та же формула редакции — один раз на ответ;
* **D2-4** язык формулы = язык ответа, а не `settings.language`.

Всё три — чистые функции, LLM не нужен, TDD офлайн.

### B. DEV-90 — РИСК ПОТЕРИ клиентских данных

`run_client_backup` фейл-клоузится тихо: нет криптоматериала → `rc = 0`,
планировщик зелёный, единственный сигнал — строка в телеграм-сводке, а сводка
в соседней арке уже была мёртвой. Итог: клиентские бэкапы не делаются, и никто
не узнает — до дня, когда они понадобятся. Класс
[[jarvis-check-that-goes-silent-when-broken]]: сегодня на нарочно убранную
переменную краснеет **ноль** проверок.

### C. Дешёвое, без риска

* DEV-98 подготовить к мержу (НЕ мержить) — ветка `devtask-20260902_...600c65`;
* план чистки 160 worktree — только план, ничего не удаляю.

## Что сознательно НЕ взял

* **keep-alive** ($114/мес на клиента). Деньги настоящие, но меньше, чем
  «бот не закрывает сделку», а приёмка экономии требует ЖИВОГО замера кэша —
  ночью его сделать нечем (запрет на платные прогоны).
* **DEV-78** (блокер переезда). Самая важная по бэклогу, но это перепись всей
  суиты — на ночь без владельца не влезает, и она не про деньги, а про приёмку.

---

## Журнал (пишется после каждого шага)

- [x] worktree создан, ветка отрезана от `a83ffb66`
- [x] мутант после убитого гейта откачен владельцем 03.09 утром; дерево чистое
- [x] ПРОГНАН мутационный гейт `scripts/mutate_sales_c1_lead_numbers.py`,
      16 мишеней. **Если строка осталась неотмеченной — прогон снят
      снаружи, и в `chatter/core/guardrails.py` (или `langdetect.py`)
      ЛЕЖИТ МУТАНТ**: `finally: revert` не отрабатывает при kill.
      Признак — mtime файла `2033-05-18` (`_MTIME_BASE`). Лечение:
      `git checkout -- <файл>`, дерево обязано стать пустым по `git status`.
      Лог прогона: `docs/night-2026-09-03/gate-run.log`.
      РЕЗУЛЬТАТ: **14 из 16 пойманы, 2 СЛЕПЫХ** (оба на D2-3, дедуп формул),
      rc=1. Дерево после гейта пустое по `git status` — откат побайтовый.
- [ ] ЗАПУСКАЮ ПОЛНУЮ суиту дважды: базлайн в `C:\jarvis_worktreesase-a83ff`
      (ветка `tmp/base-a83ffb66`) и арку здесь. Суита ДОЛГАЯ и жрёт память —
      ночью сессию сняли по 137 именно на длинном прогоне. Если строка
      осталась неотмеченной: мутанта в дереве быть НЕ ДОЛЖНО (гейт уже
      отработал и откатил), но суита могла оставить ОСАДОК в `state/` —
      сверять `git status` и `state/` перед любым следующим замером.
      Логи: `docs/night-2026-09-03/suite-arc.log`, `suite-base.log`.
