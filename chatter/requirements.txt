anthropic>=0.40
PyYAML>=6.0
telethon>=1.36
# Только для `chatter.onboard`: разбор xlsx-брифа. Раннер клиента её не
# импортирует — деплой клиента остаётся копией папки без новой зависимости.
openpyxl>=3.1
