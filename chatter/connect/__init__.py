# -*- coding: utf-8 -*-
"""`chatter.connect` — подключение клиента под ключ: от готового конфига до живого бота.

Спека: `docs/superpowers/specs/2026-08-20-t7-turnkey-connect.md` (ОК владельца 20.08).

Модуль намеренно ОТДЕЛЁН от `chatter.onboard` (решение владельца q5): онбординг
собирает файлы, connect поднимает живого клиента — разные права и разный радиус
ошибки. Импорта `chatter.onboard` отсюда нет и быть не должно: слитый модуль
однажды подключит что-нибудь прямо во время сборки.

Здесь НЕТ ни одного импорта подмодулей, и это осознанно. `model.py` обязан
импортироваться без поднятого окружения и без существующих `probes`/`actions`
(§12.1): пакетный `__init__`, тянущий `steps`, потащил бы за собой probes и
actions, и `from chatter.connect.model import Verdict` падал бы по причине,
не имеющей к модели никакого отношения.
"""
