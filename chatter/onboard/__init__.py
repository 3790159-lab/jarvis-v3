"""Пайплайн онбординга клиента: бриф (xlsx) → каталог клиента + отчёт.

Спека: `docs/superpowers/specs/2026-08-14-chatter-onboarding-pipeline.md`.
План:  `docs/superpowers/plans/2026-08-17-onboard-pipeline-plan.md`.

Главный инвариант арки: каждое правило формата, выученное на ручной сборке
клиента, становится КОДОМ и СТОРОЖЕМ, а не абзацем в памяти разработчика.

Слой T1 (`brief.py` + `form_schema.yaml`) — единственный, кто видит xlsx.
Всё, что ниже по течению, читает ТОЛЬКО `brief.json`: пока форма брифа не
зафиксирована одним артефактом, каждый следующий шаг начнёт выдумывать свои
ключи и сойдётся с соседом только на приёмке.
"""
